# VIRGIL ROADMAP

Last updated: May 8, 2026 (Track 6 #5.1 — Combat Entry Assist ships. Inaugural Doctrine §1b validated-suggester. New `srd_resolver.py` pure-function module: `resolve(creature_name, campaign_id) → SRDResult | None`, exact + Jaccard fuzzy + LLM-gated paths, `_llm_suggest` via `cloud_router` task_type='extraction', `_MONSTER_INDEX` of 334 SRD monsters from 5e-database CC-BY 4.0. `srd_monsters.json` generated by `generate_srd_index.py`. `npc_upsert()` return shape changed from `int | None` to `tuple[int, bool] | None` (was_new flag). Hook wired into `_extract_and_persist_world` — `_handle_new_npc_for_srd_suggestion` fires async on every `was_new=True` upsert, no mode gate (§11.H). `_post_srd_suggestion` posts to `#dm-aside` with `!init madd` command and `!init add` fallback. Two-line telemetry: posted=0 from resolver, posted=1 from transport. 32 tests across 3 files: `test_srd_resolver.py` (21), `test_srd_suggestion_hook.py` (6), `test_srd_index_integrity.py` (5+1002 subtests). Defense-in-depth fix: `resolve()` wraps `_llm_suggest` in try/except. Next: one session of play data, then decide #5.4 vs Track 4 #3.)

**Track 7 — Adjudication Layer** is the new track introduced this session. Architectural truth surfaced by S25 #3 multiplayer test: directives shipped through S23 (persistence, loot, redirect, footer, advisory) were advisory, not binding — under play pressure from a second player who probed limits naturally, the LLM folded on every meaningful test (failed rolls produced success narration; "says who" defeated capability refusals; player declarations became outcomes; combat narrated without Avrae state; fabricated NPCs entered combat with no stats). Track 7 #1 ships `adjudicator.py` — single pure function `adjudicate(player_input, scene_state, character, combatants, active_turn, intent) → AdjudicationResult` composed BEFORE every directive in `build_dm_context`, with five locked action categories (FREE / CHECK / CAPABILITY / COMBAT / WORLD_BOUNDARY), deterministic regex+keyword intent classifier (LLM-based rejected — would be another hallucination layer), narration constraints rendered top AND bottom of prompt (belt + suspenders per Doctrine §2 + §48). §11.K activates S9's previously-dead INVALID render branch; §11.L deduplicates redundant advisory directives when adjudication binds. Track 7 #1.1 ships `cache_autopopulate` triggered on `!sheet` post in the Avrae handler — promotes capability adjudication from 4/7 binding to 7/7 by closing the multi-PC cache fragility that left adjudication half-deaf. Track 7 #2 shipped S25 #7: multi-actor `arbitrate()` + `narration_verifier.py` — closes F-48 (concurrent input collision), F-49 (fabricated NPC combatants), F-50 (cross-player social override). Bones queue now pending Track 6 #4 only.

S25 #6 ship summary for context: Bug 3 `/travel` durability + NPC list location-scoping. Two write paths fixed in one ship; one silent regression caught mid-verification (`get_scene_state` had never SELECTed `current_location_id` since the column was added — quietly degraded the new filter to None-passthrough; fix incidentally re-activated dormant location enrichment in three other directives). `/travel` now calls `location_upsert(campaign_id, dest)` when destination doesn't pre-exist, then `set_current_location(...)` unconditionally. New `get_recently_active_npcs(campaign_id, location_id=...)` strict filter (NULL = silent, since the parser leaves NULL by default; the prior "include NULL = always-present" rule grew the always-present set with every fabricated NPC). New `npcs_in_context: campaign={N} count={K} location_filtered={0|1}` and `travel: campaign={N} from={...} to={...} resolved={0|1} created={0|1}` telemetry. Closed F-13's S25 mode; F-44 (chroma bleed: Keeper-class NPCs surviving travel through retrieval) filed v1.x as the remaining channel.

S25 #4-5 ship summary for context: Track 7 #1 + #1.1 (see opening paragraph for architectural framing). Live verification proved the layer's binding behavior — world_boundary refusals produce in-fiction non-occurrence ("words dissolve before they can shape crystal") with NO fabricated NPC justification; combat-inactive refusals prompt `!init begin`; CHECK_ACTION surfaces the right skill at the right DC band and consumes buffered rolls; CAPABILITY refusals bind for class/spell mismatches (Fireball / Sneak Attack / Rage all refused for the bard PC after #1.1 closed the cache miss). 4/7 → 7/7 jump from one startup-time fix demonstrated the architecture was deaf, not broken.

S25 #3 — Multiplayer test (data-only, no ship). Two-player live test ran ~45 minutes; eight failures catalogued as F-45 through F-52 (failed rolls succeeding, "says who" defeating refusals, player declarations narrated as outcomes, concurrent input collision, fabricated NPCs without Avrae backing, cross-player social override, combat without Avrae state, motif drift). One root: LLM in decision path for things that should be deterministic Python. Reframed the queue from world-systems (Track 4 #3+) and texture work to the adjudication layer (Track 7 #1). The earlier S25 ships (housekeeping + Track 6 #3 OOC advisory + #3.1 commands reference + #3.2 auto-generated Virgil section) remain shipped and load-bearing — Track 7 #1 reads adjudication-relevant state through helpers those ships established.

This is the living document. Status, next priorities, small wins, deferred work, decision rules. Updated every session that ships something.

For "what is the system today" → `VIRGIL_MASTER.md`
For "why did we do it that way" → `WHY.md`

---

## Status snapshot

| Phase | Description | Status |
|---|---|---|
| Track 4 #3 — Time Progression v1 (S27, May 8 2026) | New deterministic time-advancement primitive — no LLM in the decision path (Doctrine §1a). Schema: two new `dnd_scene_state` columns (`campaign_day INTEGER DEFAULT 1`, `day_phase TEXT DEFAULT 'Morning'`) via idempotent ALTER TABLE migration; new `dnd_time_advancements` audit-log table appended to `_CAMPAIGN_SCOPED_TABLES` per VIRGIL_MASTER §6 cascade requirement. Single write path: `advance_time(campaign_id, days_delta, phase_delta, source, source_detail, set_phase=None) → TimeAdvancement | None` in `dnd_engine.py`. Six-phase enum (`Morning / Midday / Afternoon / Evening / Night / Late Night`) with O(1) modular phase normalization (`total_steps = idx + phase_delta + days*6; new_day = before + total//6; new_idx = total % 6`). `set_phase` precedence invariant (§11.I): when set, ignores `phase_delta` and computes `resolved_phase_delta = (target_idx - current_idx) mod 6`; audit log row records all three values (caller-requested + caller-passed + resolved). Missing-campaign no-op contract (§8): `advance_time()` reads scene_state first, returns `None` with `err='no scene_state row'` diagnostic if absent. New pure-function `parse_elapsed(elapsed_str) → (days_delta, phase_delta) | None` for free-text duration → delta mapping (deterministic regex/keyword table; ~6h per phase; always-fire `parse_elapsed:` log line). New seventh §59 directive sibling `compute_time_directive(scene_state, just_advanced) → str` in `dnd_orchestration.py` — fires non-empty only when `time_just_advanced(campaign_id, window_seconds=60)` returns True (recency check on `dnd_time_advancements` MAX(created_at) per §11.E sub-(iii)α). `render_state_footer` extended with ` · Day {N}, {Phase}` suffix after the mode glyph; `signals` dict gains `campaign_day` + `day_phase` keys; `state_footer:` log line extended with `day=` / `phase=`. Backward compat: legacy scene_state with no time fields renders as before (test 28 pinned). Four call sites for `advance_time`: `/travel` (after location write — `parse_elapsed(elapsed)` only; `arrival_time` display-only per §11.G=b lock); Avrae `!lr` hook (`+1 day, set_phase='Morning'`); Avrae `!sr` hook (`+0 day, +1 phase`); new `/advance days:N phases:N set_phase:Phase` DM-only slash command. Skeleton-loader seed: optional `## Starting time` section (`day=N`, `phase=NAME`); `apply_starting_time_seed(campaign_id)` writes `dnd_scene_state.campaign_day` / `day_phase` directly per §J.3 narrow §17 exception (initialization-only, idempotency-guarded — only fires when scene_state is at defaults `(1, 'Morning')`); does NOT route through `advance_time()` and does NOT append to `dnd_time_advancements`. Wired into `/play` post-`init_scene_state` on first session. Always-fire telemetry: `time_advance: campaign={N} source={...} before={d,p} after={d,p} days_delta={N} phase_delta={N} [resolved_phase_delta={N} set_phase={...}] detail={...}` per advancement; `parse_elapsed: input={...} result={d,p|none}` per parse; `directive_emit:` extended with `time={1|0}`. `_CAMPAIGN_SCOPED_TABLES` now 12 entries (was 11). Six new test files (~89 assertions): `test_parse_elapsed.py` (27), `test_advance_time.py` (29 incl. set_phase precedence + large-`phase_delta` 12/13/25 + cascade integrity + time_just_advanced recency), `test_compute_time_directive.py` (9), `test_render_state_footer_time.py` (8), `test_time_skeleton_seed.py` (9), `test_time_schema_integrity.py` (8 incl. `/purgecampaign` cascade integrity). Pre-existing `test_campaign_delete_cascade.py` fixture extended to include all 12 scoped tables (was missing 4 pre-existing + 1 new); all 13 cascade tests pass. Spec lock at `/home/jordaneal/virgil-docs/TRACK_4_3_SPEC.md` v1.2 (§11.A–§11.I all LOCKED). **First post-checkpoint Motion-Systems thread ship — addresses F-54 surface 1 (the world doesn't visibly evolve).** | ⏳ LIVE VERIFY PENDING — 124 pytest + 44 standalone (168 total) green; bot service active post-restart with clean migration on live DB (columns + table + indexes verified via PRAGMA / .schema). 8-step canonical scenario in `tests-to-run-post-session.md` covers /play footer baseline, /travel +N day advance, arrival_time display-only, Avrae !lr / !sr advance, /advance days/phases/set_phase, /purgecampaign cascade. Promote to ✅ SHIPPED LIVE after run. |
| Track 6 #5.1 — Combat Entry Assist (May 8, 2026) | New `srd_resolver.py` pure-function module — `resolve(creature_name, campaign_id) → SRDResult | None`. Three-tier resolution: (a) exact key match in `_MONSTER_INDEX`, (b) Jaccard token-overlap fuzzy match (threshold 0.6), (c) LLM suggester via `cloud_router` task_type='extraction' (Doctrine §1b — LLM proposes; `_MONSTER_INDEX` validates candidate exists; confidence gate ≥0.65; DM approves in Discord). `SRDResult` dataclass: `input_name`, `srd_name`, `cr`, `hp`, `ac`, `confidence`, `method`. `_LLM_CACHE` cache-poisoning fix: transient exceptions do NOT write to cache; genuine no-match responses do. `_SUGGESTED` set: session dedup keyed by `(campaign_id, name_lower)`. Two-line telemetry shape: `srd_suggestion:` with `posted=0` from `resolve()`; second line with `posted=1` from `_post_srd_suggestion()` after Discord send. `generate_srd_index.py` fetches 5e-database `src/2014/en/5e-SRD-Monsters.json` (334 monsters, CC-BY 4.0) and builds `srd_monsters.json` with `_meta` attribution key. `npc_upsert()` return shape changed from `int \| None` to `tuple[int, bool] \| None` — `was_new=True` on INSERT (fires hook), `False` on UPDATE (primary dedup guard). Hook wired: `_extract_and_persist_world(campaign_id, narration, guild=None)` — `asyncio.ensure_future(_handle_new_npc_for_srd_suggestion(...))` fires for every `was_new=True` NPC. **No mode gate (§11.H locked)** — hook fires regardless of scene mode; narration fires before `!init begin` so `mode='combat'` gate would have silenced all suggestions. `_post_srd_suggestion` posts `!init madd "{srd_name}" -name "{input_name}"` + `!init add 0` fallback to `#dm-aside`; soft-fail throughout — any exception logged and swallowed. 32 tests across 3 files (21 + 6 + 5 with 1002 subtests). Defense-in-depth fix added: `resolve()` wraps `_llm_suggest` call in try/except. Spec: `TRACK_6_5_1_SPEC.md` v1.2 locked; review: `TRACK_6_5_1_REVIEW.md`. **Inaugural Doctrine §1b validated-suggester ship.** | ⏳ LIVE VERIFY PENDING — 32/32 tests pass, bot boots clean (`srd_resolver: index loaded entries=334`), no real Discord turn yet. Walk 5-step scenario in `tests-to-run-post-session.md` §Track 6 #5.1: narrate a monster → check `#dm-aside` for suggestion card → grep `srd_suggestion:` for two-line shape. Promote to ✅ SHIPPED LIVE after. |
| Track 6 #4 — NPC Stat Hydration at Init-Add (May 7, 2026) | New `npc_hydrator.py` — pure-function CR-band lookup (16 bands, CR 0–12), `hydrate_npc_stats(cr_str) → dict`, `normalize_cr()`, `fallback_stats()`. Eight nullable stat columns added to `dnd_npcs` via PRAGMA+ALTER migration (`hp_max`, `ac`, `attack_bonus`, `damage_dice`, `save_bonus`, `init_mod`, `cr_str`, `avrae_source`). New engine helpers: `stat_incomplete(npc) → bool`; `npc_hydrate_stats(campaign_id, name, cr_str, source) → (bool, dict)` — source-based fill: hook sources use idempotent NULL-fill (WHERE col IS NULL), `explicit_hydrate` always-overwrites (§11.H lock); `generic_fallback` writes all stats except `hp_max`; `npc_register_avrae_madd(campaign_id, name) → (bool, dict)` — registers `!init madd` creatures with `avrae_source='avrae_madd'`, leaves stat cols NULL. `_NPC_COLS` extended to include all 8 new columns; `_npc_row_to_dict` updated. `avrae_listener.parse_init_list_embed` now extracts `status_token` per combatant row (`<None>`, `<Healthy>`, etc.). `_handle_init_list_event` extended with step 2.5 — classification scan: bound PCs → `source=bound_pc_skip`; `status_token != '<None>'` → `npc_register_avrae_madd`; `status_token == '<None>'` → hydration path (cr_hint known → hook; cr_hint None → `_post_hydration_prompt` to `#dm-aside` THEN `generic_fallback`). New `/hydrate` DM-only slash command — validates CR via `normalize_cr`, calls `npc_hydrate_stats(source='explicit_hydrate')`, ephemeral stat-block response, no sync hint (§11.D). `directive_emit` extended with `hydration_write_fired={0|1}`. Always-fire log lines: `hydration:` per combatant per parse (8 source values); `hydration_manual:` per `/hydrate` invocation. 5 new test files: `test_npc_hydrator.py` (24), `test_npc_hydrate_stats.py` (17), `test_npc_register_avrae_madd.py` (7), `test_hydration_hook.py` (20), `test_slash_hydrate.py` (12). **Closes F-49 operational gap: verification refuses fabrication; hydration enables legitimate addition. Bones queue now EMPTY.** **v1.x candidate:** hydration prompt extended with LLM-suggested CR (§1b — deterministic CR-band validator gates the suggestion, DM approves) instead of asking DM cold. Filed in §12 of TRACK_6_4_SPEC.md. | ✅ SHIPPED LIVE |
| Track 7 #2 — Multi-actor Arbitration + Narration Verification (S25 #7, May 7 2026) | New `arbitrate(actions, scene_state, characters, combatants, active_turn, intent_per_actor, avrae_events, skeleton_capabilities, character_cache) → ArbitrationResult` in `adjudicator.py`. All-pairs conflict detection (§11.R — every pair (i,j) scanned, not adjacent-only); text-aware contradiction detection (FREE verdicts override binding verdicts only when the free text explicitly asserts an opposing outcome — "he agrees with us" vs CHECK fail, "the room ignites" vs CAPABILITY refusal; "I attack" and "I help him" are non-contradictory); per-actor sibling deduplication (§11.O); cache-miss fallback (`no_character_context` for unknown actors, §11.P). `ArbitrationResult` dataclass: `verdicts: list[AdjudicationResult]`, `actor_order: list[str]` (CHARACTER NAMES, §11.Q), `merge_plan: 'sequence'\|'override'`, `primary_actor`, `combined_constraint`, `overridden_actors: list[str]` (§11.R), `signals: dict`. New module `narration_verifier.py` — parallel sibling per Doctrine §63 (fork, not extension of `adjudicator.py`). `verify_narration(narration_text, arbitration_result, scene_state, combatants, npcs_canonical) → VerificationResult`. Four locked violation classes (§11.F): `FABRICATED_COMBATANT` (NPC name in narration not in combatants/bound-PCs/skeleton — closes F-49 Silent Beast / Keeper of the Vein channel); `VERDICT_CONTRADICTION` (narration asserts outcome opposing binding verdict); `STATE_MUTATION_CLAIM` (damage/HP claim while combat inactive); `ACTOR_OMISSION` (arbitrated non-cache-miss actor wholly absent from response — closes F-48 silent-drop channel). First violation wins. 1-retry loop; second failure → deterministic escalation placeholder per §6.3 per-category branches. Feature flags: `ARBITRATION_ENABLED` (short-circuits to first-actor-only), `VERIFICATION_ENABLED` (fail-open bypass). Always-fire log lines: `arbitration: campaign={N} actors={N} primary='{name}' merge_plan={...} overridden={N}`; `verification: campaign={N} passed={0\|1} violation_class={...} retry_fired={0\|1} escalated={0\|1}`. `build_dm_context` kwarg renamed `adjudication_block` → `arbitration_block` (§11.N); block header `=== ADJUDICATION RESULT ===` → `=== ARBITRATION RESULT ===`. `discord_dnd_bot.py`: `_dm_respond_and_post` now passes structured `actions` list to `dm_respond`. New tests: `test_arbitration.py` (21 assertions), `test_narration_verifier.py` (40 assertions), `test_dm_respond_arbitration.py` (9 assertions). Spec lock at `/home/jordaneal/virgil-docs/TRACK_7_2_SPEC.md` (§11.A–R all LOCKED). **Closes F-48 (concurrent input collision), F-49 (fabricated NPC combatants), F-50 (cross-player social override). Bones queue now pending Track 6 #4 only.** | ✅ SHIPPED LIVE |
| Bug 3 — `/travel` durability + NPC list location-scoping (S25 #6, May 7 2026) | `/travel` now calls `location_upsert(campaign_id, dest)` when destination doesn't pre-exist, then `set_current_location(campaign_id, dest_loc['id'])` unconditionally. Previously `set_current_location` only fired on `dest_resolved=True`; unknown destinations updated only the embed footer (one-shot `label_override`) while `current_location_id` reverted on the next `build_dm_context` read. Closes F-13's S25 mode (Veiled Spire ↔ Old Tavern revert observed live in S25 #3 multiplayer test). New `get_recently_active_npcs(campaign_id, location_id=...)` strict filter — only NPCs with matching `location_id`. NULL `location_id` is silent (parser leaves NULL by default; "include NULL" rule grew the always-present set with every fabricated NPC). `build_dm_context` reads `scene_state.current_location_id` and passes it. Scopes "Recently active NPCs" prompt block to current location. **`get_scene_state` regression caught and fixed mid-ship**: function had never SELECTed `current_location_id` so its returned dict had no such key; new filter quietly degraded to None-passthrough. Same dict feeds consequence directive (line 4779), commitment directive (line 4831), and init directive (line 4856) — all three already had `current_loc = scene_state.get('current_location_id')` guards followed by `at_location` enrichment that had been silently dormant since the column was added. Bug 3 fix incidentally re-activates them. New telemetry: `travel: campaign={N} from={...} to={...} resolved={0|1} created={0|1}`; `npcs_in_context: campaign={N} count={K} location_filtered={0|1}`. New `test_travel_persistence.py` (25 assertions); `test_dnd_npcs.py` extended (10 new strict-filter cases). All adjacent suites green. **Live verified S25 09:07–09:08**. **Filed v1.x**: F-44 chroma bleed — past-location turns can still surface as RELEVANT PAST EVENTS even after travel; Bug 3 closed the prompt-block channel, chroma is the remaining channel. | ✅ SHIPPED LIVE |
| Track 7 #1.1 — Cache Auto-Populate (S25 #5, May 7 2026) | `cache_autopopulate` triggers on `!sheet` post in Avrae handler — when an Avrae `!sheet` embed is detected for a bound PC, the cache write fires immediately. No restart, no `/refresh`, no operator hop. New `cache_warm_incomplete` diagnostic at startup if any bound PC's most-recent `!sheet` post is older than 300 messages (telemetry only — doesn't block startup). Adjudication's `_gate_capability` no longer returns `no_character_context` deferral as the dominant path — with cache populated for all bound PCs, capability gates evaluate against real character data. **Live verified S25 #5** — same three prompts as Track 7 #1 capability tests (Fireball / Sneak Attack / Rage) post-fix all logged `actor='Jordonovan Bigsby' category=capability allowed=0 refusal_kind=capability`; pre-fix all three had `actor='?' allowed=1`. **Effective category coverage now 7/7 binding.** Track 7 #1's architecture proven: 4/7 → 7/7 jump from one startup-time fix demonstrates the layer was deaf, not broken. | ✅ SHIPPED LIVE |
| Track 7 #1 — Adjudication Layer v1 (S25 #4, May 7 2026) | New `adjudicator.py` module. Single entry point: `adjudicate(player_input, scene_state, character, combatants, active_turn, intent) → AdjudicationResult`. Pure function. Composes BEFORE every directive in `build_dm_context`. Five locked action categories: `FREE_ACTION` (pure RP, no stakes); `CHECK_ACTION` (outcome uncertain — resolver picks skill + DC band easy/medium/hard, consumes `RollBuffer` event when present, narration constrained to success/failure shape); `CAPABILITY_ACTION` (class/level/spell/feature gates — refusal binding, no "says who" override); `COMBAT_ACTION` (requires `mode='combat'` AND populated init order; refuses with explicit prompt to use `!init begin` if combat inactive); `WORLD_BOUNDARY_ACTION` (reality-violating actions — hard refusal, no negotiation, no fabrication of justification). `AdjudicationResult` dataclass with `category`, `allowed: bool`, `refusal_kind`, `roll_required`, `skill`, `dc_band`, `roll_consumed`, `narration_constraint`, `signals`. Deterministic intent classifier (regex + keyword vocabulary, Appendix A in spec) — five surfaces: world-boundary patterns, combat verbs, capability claims, check verbs, default fallback. Crude beats clever — LLM-based classification rejected. **Narration constraints rendered top-of-prompt** (`=== ADJUDICATION ===` block) AND bottom-of-prompt (last cache before generation) — belt + suspenders per Doctrine §2 + §48. **§11.K activated:** capability adjudication produces INVALID verdicts that flow into S9's existing render branch (previously dead code). **§11.L deduplication:** when adjudication produces a binding constraint, redundant advisory directives suppress their independent narration. New per-turn `adjudication: campaign={N} actor={...} category={...} allowed={0|1} refusal_kind={...} skill={...} dc_band={...} roll_consumed={0|1}` telemetry. Spec lock at `/home/jordaneal/virgil-docs/ADJUDICATION_LAYER_SPEC.md` with twelve §11 decisions resolved, Appendix A vocabulary scope, Appendix B implementation notes. **Live verified S25 #4** — 4/7 categories binding live (world_boundary, combat-inactive, check, free); 3/7 capability tests deferred to existing HARD STOP RULES due to pre-existing `cache_warm` not loading second bound PC. Architecture proven sound; cache fix shipped as #1.1 promoting binding to 7/7. | ✅ SHIPPED LIVE |
| Track 6 — OOC Advisory Lane v1 (Track 6 #3, May 6 2026) | Read-only OOC Q&A handler for `#dm-aside` in `discord_dnd_bot.py`. New `_advisory_respond(message)` branches in `on_message` BEFORE the `#dm-narration` gate; pulls campaign / scene / active turn / combatants / inventory / pending loot via pure-read engine helpers; calls new `build_advisory_context(...)` (pure function in `dnd_orchestration.py`) for a flat state-reference block; routes via new `task_type='advisory'` in `cloud_router.py` (same provider pool as `chat`/`general`, no DnD anti-repetition penalties) with new `ADVISORY_SYSTEM_PROMPT` (OOC framing, forbid scene-narration / state mutation / `!`-prefixed Avrae emission / world invention / tactical coaching). Reply posted to `#dm-aside` with truncate-with-pointer at 1900 chars; failures fall back to a generic "try again" message + `advisory_respond_failed:` log. **Invariants** (load-bearing, in VIRGIL_MASTER §4): no `chroma_store` / no `chroma_search` (Track 4 #2 v1.2 contamination lesson — OOC noise must not seed future narrative retrieval; structural defense via `dnd_orchestration.py` not importing chroma); no engine writers; no `!`-prefixed bot emission; no tactical directives. Telemetry: `advisory_respond:` per request. 20 assertions in `test_advisory.py`. **Live verified S25 09:17–09:20**. | ✅ SHIPPED LIVE |
| Track 6 #3.1 — Advisory Command Reference v1 (S25 #2, May 6 2026) | New `COMMANDS.md` at `/home/jordaneal/virgil-docs/` (~8.4KB canonical Virgil + Avrae command reference). Loaded fresh into advisory mode on every request via `_load_commands_reference()` in `dnd_orchestration.py`. **No caching** — Avrae-section edits take effect with no restart. New `commands_reference` kwarg on `build_advisory_context(...)`. `ADVISORY_SYSTEM_PROMPT` extended with "use AVAILABLE COMMANDS as source of truth / say 'not available' rather than guess". `advisory_respond:` extended with `commands_ref_loaded` and `commands_ref_chars`. 11 new tests in `test_advisory.py` (now 31 total); new `test_commands_doc.py` (8 sanity tests). | ✅ SHIPPED LIVE |
| Track 6 #3.2 — Auto-Generated Virgil Section (S25 #2, May 6 2026) | New module `commands_doc_generator.py` — `_strip_category_tag` / `_categorize` / `introspect_virgil_commands(bot)` / `render_virgil_section(rows)` / `update_commands_doc(bot, path)`. Idempotent atomic write between `<!-- VIRGIL_AUTO_GENERATED:* -->` markers in `COMMANDS.md`; everything outside the markers preserved byte-for-byte. Wired into `on_ready` after `bot.tree.sync()`. Re-tagged `/setup` as `[SETUP]` and three list subcommands (`clock`/`quest`/`companion list`) as `[PLAYER]` so source-of-truth tagging matches intended categories — pattern locked: tag is the source of truth, generator just renders. New `commands_doc_update:` per-startup log. 33 unit tests in `test_commands_doc_generator.py`. **Live verified S25 #2** with all five test cases (probe injection lifecycle, doc count delta on retag, Avrae section byte-preservation across restarts, `commands_ref_loaded=1` post-restart). New doctrine candidate (§66): doc auto-generation as drift defense — when a doc duplicates structured information already present in code, generate the duplicated portion from the code source on a deterministic trigger; preserve hand-edited regions via marker-bracketing. | ✅ SHIPPED LIVE |
| S25 housekeeping — `/setup` extensions (May 6 2026) | Three small ships in one pass: (1) **Avrae permissions reconciled** across all canonical text channels — full read+send except `#lore-notes` which is read-only (DM-only write surface, contamination defense for the bot/DM private notes lane); per-channel policy via new `AVRAE_READ_ONLY = {'lore'}` set + `chan_key` threaded through `canonical_overwrites_dict` / `ensure_canonical_overwrites`. (2) **New `#commands` channel** under `💬 OUT OF CHARACTER` — sanctioned home for slash commands and Avrae bookkeeping (convention not enforcement; slash commands work everywhere). (3) **New `AFK` voice channel** under `🔊 VOICE` + guild AFK auto-move config via `guild.edit(afk_channel=, afk_timeout=1800)` — 30 min idle → auto-move. New per-channel `avrae_perms:` log; `setup_run:` extended. 42 assertions in `test_setup_plan.py` (was 26). All adjacent suites green. Naming choice: `#commands` not `#ooc-commands` — the OOC category prefix made the channel name redundant. | ✅ SHIPPED LIVE |
| Phase 1 | Roll Discipline + Sheet + Scene + Modes | ✅ COMPLETE |
| Phase 2 | Combat / Observability / Narrative Fidelity | ✅ COMPLETE |
| Phase 3 | Suggestion + Auto-Execute | ✅ VALIDATED LIVE |
| Phase 10 | Model swap, coverage audit, perm unlock | ✅ COMPLETE |
| Phase 11.1 | Mechanical Hints (advisory parser) | ✅ SHIPPED LIVE |
| Phase 12 | Persistent canon (NPCs, locations, skeleton) | ✅ SHIPPED LIVE |
| Ship 1 | `/travel` + location surfacing | ✅ SHIPPED LIVE |
| Ship 2 | Companion autocomplete + quest reminders | ✅ SHIPPED LIVE |
| Ship 4 | Phantom-location telemetry + `world_health` aggregate | ✅ SHIPPED LIVE |
| Campaign management | `/setcampaign`, `/deletecampaign` (batch), `/archived`, `/purgecampaign`, `/purgeallcampaigns` | ✅ SHIPPED LIVE |
| Roll discipline drift fix | Mode-aware `classify_action_intent` + RISKY_RX negative lookaheads | ✅ SHIPPED LIVE |
| S9 — Equipment grounding (v1) | 3-state CapabilityVerdict + skeleton hint layer + UNVERIFIED narration directive | ✅ SHIPPED LIVE |
| Bot startup auto-cache (S14) | `_warm_character_cache_on_startup` scans recent history for Avrae sheets | ✅ SHIPPED LIVE |
| `virgil-discord` sentinel monitoring | Added to `sentinel.sh` health checks | ✅ SHIPPED LIVE |
| COMBAT_RX false-positive cleanup | Dropped `fire`/`shoot`/`loose` from combat verb list | ✅ SHIPPED LIVE |
| Track 3 — Pacing directive | tension/clocks → imperative narrative-move constraints | ✅ SHIPPED LIVE |
| Track 3 — Central thread directive | First skeleton hook → directional pressure | ✅ SHIPPED LIVE |
| Track 3 — DM philosophy layer | Authored doc, mtime-cached, frames directive interpretation | ✅ SHIPPED LIVE |
| Track 4 — Corpus inventory | CRD3 + FIREBALL reconnaissance complete | ✅ DATA COLLECTED |
| Donovan/Ruby address-name fix (S15) | `primary_name` property on `CharacterContext` + ACTING CHARACTER prompt directive | ✅ SHIPPED LIVE |
| PC contamination guard (S15) | `names_overlap` helper + extractor `pc_match` filter + engine `npc_upsert` refusal | ✅ SHIPPED LIVE |
| `_INVOCATION_VERBS` `equip`/`use` (S15) | Added to capability claim regex | ✅ SHIPPED |
| `parse_avrae_sheet_embed` !coin/!item refusal (S15) | Markdown-aware first-line + all-defaults sentinel | ✅ SHIPPED |
| Phase 6 — Actor name reconciliation (S15) | Strict-only canonical/alias resolution + cache warming on bot startup | ✅ SHIPPED LIVE |
| Track 3 — Consequence surfacing v1 (S16) | `dnd_consequences` ledger, dual-pass parsers (`consequence_extractor.py`), severity-then-recency cap-at-3 directive, promotion-with-distribution-check, `/consequence list [npc]` debug, 249 new test assertions | ✅ SHIPPED LIVE |
| Cascade fix for `dnd_consequences` (S16) | `_CAMPAIGN_SCOPED_TABLES` updated — child of `dnd_npcs`, listed first | ✅ SHIPPED |
| `/consequence` group restructure (S16) | Single command flipped to `app_commands.Group` matching `/quest` / `/clock` / `/skeleton` pattern. UX: `/consequence list [npc:NAME]` | ✅ SHIPPED LIVE |
| Consequence list formatter rewrite (S16) | Bold NPC, kind+sev inline, `status` in backticks, sources/surf/T-tag meta strip, italics summary on indented continuation. No more padded-whitespace columns. | ✅ SHIPPED LIVE |
| `consequence_health_report` + composed into `world_health_report` (S16 autonomous block) | Per-turn telemetry: `cons_active`, `cons_promoted`, `cons_never_surfaced`, by-kind and by-source breakdowns. 5 new tests in `test_world_health.py`. | ✅ SHIPPED LIVE |
| `extract_scene_updates` task_type fix (S16 autonomous block) | Was `task_type="dnd"` — competing with the actual narration call for groq_heavy each turn AND polluting DND_PRIORITY_OVERRIDE log noise. Now `task_type="extraction"` (cerebras/groq fallback), matching every other JSON-extraction parser. Removes redundant heavy-model calls per turn. | ✅ SHIPPED LIVE |
| `godmode_gap` diagnostic log (S16 autonomous block) | `dm_respond` logs when `intent=COMBAT` fires in non-combat mode. No behavior change — surfaces the gap empirically before any constraint layer ships. Feeds the Committed Action Resolution spec. | ✅ SHIPPED LIVE |
| Empty-narration diagnostic log (S16 autonomous block) | `dm_respond` logs when LLM returns empty/whitespace pre-clean OR cleaned response is too short to narrate post-auto-execute. Surfaces the "footer-only with no narration" failure mode Jordan saw at 20:41 May 3. | ✅ SHIPPED LIVE |
| S21 — OOC contamination guard (consequence player parser) | Leading-marker filter (`((`, `[OOC]`, `OOC:`, `//`) short-circuits `parse_consequences_player` before LLM call. Diagnostic `consequence_ooc_filtered: campaign={N} reason={paren\|bracket\|colon\|slash} text={first 80}` measures marker prevalence. Player-channel only — DM parser unaffected. 46 new test assertions in `test_consequence_extractor.py`. | ✅ SHIPPED LIVE |
| S22 — unconsumed roll sweep diagnostic (`avrae_listener.py`) | `RollBuffer._sweep()` now logs every TTL-expired event before deletion: `unconsumed_roll_swept: actor='{actor}' action='{action}' age_s={age}`. Added module-level `log()` to `avrae_listener.py` (mockable in tests). Pure visibility — no behavior change. 11 assertions in `test_avrae_sweep.py`. | ✅ SHIPPED |
| S23 — near-match fragmentation log on canonical writes | `npc_upsert` and `location_upsert` INSERT branches now query existing canonical names and log `npc_near_match` / `location_near_match` when Levenshtein distance ≤ 2 against any existing row in the same campaign. `levenshtein_distance()` helper added to `dnd_engine.py`. Pure observability — no behavior change, no auto-merge, Phase 6 strict-equality identity rule untouched. 26 assertions in `test_npc_near_match.py`. | ✅ SHIPPED |
| S24 — per-turn prompt size telemetry | `dm_respond` now logs `prompt_size: campaign={N} system={chars} retrieval={chars} party={chars} scene={chars} directives={chars} total={chars}` after `build_dm_context` + all prepends. Section breakdown from input variables. Pure measurement. 22 assertions in `test_prompt_size.py`. | ✅ SHIPPED |
| S25 — directive emission summary log | `dm_respond` now logs one `directive_emit:` line per turn: `campaign={N} pacing={tier|none} central_thread={1|0} philosophy={chars} consequence={count} capability={verdict|none} commitment=0`. Single signal for threshold calibration alongside existing per-directive logs. 23 assertions in `test_directive_emit.py`. | ✅ SHIPPED |
| B2 — `!attack` template directive (Session 18 live-test follow-up) | `RollDecision.to_prompt_directive()` for `category='attack'` now emits a fill-in template `!attack "<weapon-name>" -t <target>` (with `!cast <spell-name> -t <target>` for spells), explicitly flagging `<No Target>` as the failure mode if `-t` is omitted. HARD STOP RULE 5 in `dnd_engine.py` carved out for attack templates. Pre-fix the bare `category='attack'` `RollDecision` produced `cmd='!roll'` while the reason field told the LLM to use `!attack` — LLM freelanced bare `!attack` and Avrae rolled against `<No Type>`. Diagnosed live during S22-S25 test pass. 30 assertions in `test_attack_directive.py`. | ✅ SHIPPED |
| S22–S25 live verification (Session 18 test pass) | All four observability ships verified live in Discord against campaign 17. S22 fired with 188.7s aged-out roll. S24 surfaced 24-25k char prompts every turn (above the 18k bloat threshold; correlates with the empty-narration failure mode at `prompt_chars=23789`). S25 stable across 5 turns showing `pacing=none central_thread=1 philosophy=3343 consequence=1-2`. S23 fired live on second test pass (`new='Garrik' existing='Garrick' distance=1`, NPC id=11 inserted) once a single-word capitalized stranger was introduced — first pass missed because DM auto-corrects player typos and `bad_name_format` validator rejects titled names like "Garrik the Younger". Doc `tests-to-run-post-session.md` greps fixed: `journalctl -u` → `--user -u` (user unit, not system unit). | ✅ VERIFIED LIVE |
| B2.1 — attack directive narration mandate + no-quotes correction | Live-test follow-up to B2: pre-fix narration mandate, the LLM produced ONLY the templated `!attack "unarmed strike" -t Garrick` command (35-char total response, no narration before it). Two issues: (a) quotes around multi-word weapon names are wrong — Avrae uses positional parsing (`!attack unarmed strike -t Garrick` is correct, matching the existing `!check sleight of hand` convention); (b) the directive language was so prescriptive about command shape that the LLM treated it as the entire required output. Fix: removed quotes from the template, added explicit `Your message MUST narrate the player's attempt BEFORE the command` mandate flagging command-only responses as INSUFFICIENT, plus a `DO NOT wrap multi-word names in quotes` line with positive/negative examples. HARD STOP RULE 5 carve-out updated to match. 36 assertions in `test_attack_directive.py` (was 30). Verified live: narration returned + correct unquoted syntax + `-t Garrick` target binding emitted. | ✅ SHIPPED |
| Track 3 — Committed Action Resolution v1 (escape-only, S19) | New `compute_commitment_directive` in `dnd_orchestration.py`: pure function gating on COMBAT prior intent + scene-shift current intent + Avrae buffer not drained + reaction verbs absent in prior DM response + no explicit retraction → emits imperative directive demanding the LLM resolve, refuse, or charge for the new action. New `SCENE_SHIFT_RX` / `_REACTION_VERBS` / `_RETRACTION_RX` regex sets, `is_scene_shift_intent` / `_has_reaction_verbs` / `_is_retracting` helpers, `commitment_log_summary`. Engine: new `last_dm_response TEXT DEFAULT ''` column on `dnd_scene_state` (idempotent migration), `update_last_dm_response` single writer (called between cleaned narration emission and turn-counter increment), `get_npc_names_at_location` helper for §11.B target hints. `build_dm_context` plumbed with `commitment_directive=` kwarg, renders `=== UNRESOLVED COMMITMENT ===` block AFTER consequence (locked §11.8). New telemetry: `commitment_directive:` per-turn aggregate (fires every turn with `fired={1\|0} prior_intent= current_intent= is_scene_shift= avrae_drained= reaction_verbs= retraction_filtered=` for empirical baseline) + `commitment_retraction_filtered:` only when retraction grammar suppresses + `directive_emit:` `commitment=` field promoted from `0` placeholder to `{1\|0}`. 82 assertions in `test_commitment_directive.py`. **Live verified S19 14:28:** canonical "swing dagger at Garrick → head outside to help child" scenario produced `fired=1 prior_intent=combat current_intent=social is_scene_shift=1 avrae_drained=0 reaction_verbs=0 retraction_filtered=0`. **Layer-3 (Avrae init binding) and layer-4 (deterministic mode flip) deferred** to sibling Combat Initiation Orchestration spec per locked §11.A / §11.4. | ✅ SHIPPED LIVE |
| Track 4 — Narrative Inventory v1 (S22) | New `dnd_inventory` schema (`id`, `campaign_id`, `character_name`, `item_name`, `quantity`, `metadata`, `created_at`; INDEX on `(campaign_id, character_name, item_name)`). Replaced a vestigial stub schema (`character_id` FK, no code references, undocumented in §7) per Jordan's call — campaign+name keying matches the rest of the project's pattern. Single-writer pair `add_item` / `remove_item` in `dnd_engine.py`: increment-on-existing semantics; refuses non-positive quantities and over-removal (`action='insufficient'`). Pure reads `get_inventory` (alphabetical) / `has_item` (case-insensitive). `_normalize_item_name` lowercases + collapses whitespace — `"Silver Key"` and `"silver key"` and `"SILVER KEY"` all collapse to one row. New `_bound_character_autocomplete` helper queries `get_characters(campaign_id)` and surfaces names in both `/inventory character:` and `/giveitem character:` parameters. `/inventory [character]` is player-accessible (defaults to caller's bound character via `get_character_by_controller`); `/giveitem <character> <item> [quantity]` is DM-only via `is_dm_or_creator`. Both ephemeral. New `acting_character_names` kwarg threaded through `dm_respond → build_dm_context`; render block uses it as the actor identity source so the inventory section appears even when Avrae sheet caching is missing (caught in live test — `dm_respond: no cached context for 'Donovan Ruby'` had silently skipped the render until this fix). Cap-at-8 with `... +N more` truncation. Telemetry: `inventory_add: campaign=N character=X item=Y qty=Z action=inserted|incremented`, `inventory_remove: ... action=decremented|removed|insufficient|not_found`, `inventory_give: ...` (slash-command surface), `inventory_render: campaign=N character=X count=N` (per-turn). Added to `_CAMPAIGN_SCOPED_TABLES` cascade list. **Phase 5 capability integration deferred** — `WEAPON_CLAIM_RX` is weapon-only, doesn't fire on item nouns; `ITEM_CLAIM_RX` design (verb taxonomy + preposition handling + false-positive guards) earns its own spec. Per Jordan's call, v1 ships the prompt-context render as the load-bearing surface and files the verdict-shift layer for v1.x. **Live verified S22 23:01:** `inventory_add ... action=inserted` on `/giveitem Donovan Ruby silver key`, `inventory_render: count=1` on the next dm_respond turn, prompt grew ~400 chars from the new section, LLM took the cellar/silver-key cue and ran with the cellar narrative (NPC parser surfaced `Garrick (wielding short sword) in cellar` + `Eldrin (archer) in cellar` from the LLM's response). 31 assertions in `test_inventory.py`; all adjacent suites green. | ✅ SHIPPED LIVE |
| Discord channel cleanup + canonical `/setup` (S23 #3) | Consolidated 7-channel pre-S23 layout (`dm-narration`, `dice-rolls`, `character-sheets`, `party-loot`, `lore-notes`, `ooc-general`, `bot-commands`) into 4 canonical text channels (`dm-narration`, `dm-aside`, `lore-notes`, `party-chat`) + 1 voice channel (`General`) across 3 categories (`🎲 VIRGIL DM`, `💬 OUT OF CHARACTER`, `🔊 VOICE`). **Drops** rolls/sheets/loot/commands — Avrae listener filters by author not channel (rolls in `#dm-narration` work for free), DDB owns sheets, loot narrated in `#dm-narration`, slash commands work anywhere. **Renames** `ooc-general` → `party-chat` (canonical name in code; operator renames in Discord manually). **Adds** `dm-aside` for Track 6 #3 advisory mode (channel reserved with full bot perms; routing not yet wired — existing line-594 routing gate already silently ignores non-narration text channels, so `#dm-aside` has correct no-op behavior for free until Track 6 #3 lands). New `compute_setup_plan(text_channels, voice_channels, categories, ...) → plan` in `discord_dnd_bot.py` — **fifth pure-function-planner instance** (after the four narrative-directive siblings: `compute_persistence_directive`, `compute_loot_directive`, `render_state_footer`, `compute_combat_redirect_directive`). The pattern is now codified across narrative directives AND infrastructure ops; future ops commands (campaign export, role provisioning, channel migration) have a clean template. Plan dict shape: `categories_to_create`, `text_channels_to_create`, `voice_channels_to_create`, `text_channels_to_move`, `voice_channels_to_move`, `text_channels_existing`, `voice_channels_existing`, `categories_existing`, `legacy_category_to_delete`. `/setup` rewritten to use planner: builds guild snapshot (existing categories set, text channels dict {name → category|None}, voice channels dict {name → category|None}), calls planner, executes via discord API in this order: create canonical categories first (so subsequent channel ops can reference them); create text channels with topic + permissions in target category; move existing canonical text channels to target categories via `channel.edit(category=...)`; repair bot perms on canonical-but-existing entities; create voice channels; move existing voice channels; delete empty legacy `🎲 D&D` category if no channels remain in it post-move. `#lore-notes` permissions: `@everyone` has `view_channel + read_message_history` but `send_messages=False` (DM/admins write via Discord's manage-channels override; bot keeps full REQUIRED_PERMS). Bot perm preservation: existing explicit allows are kept (never demoted) per the prior `merge_bot_overwrite` pattern. Idempotency contract: re-running on a fully-canonical guild produces a plan with all empty action-lists and full existing-lists; user-facing ephemeral renders "Nothing to do — already canonical. Bot perms repaired." Soft-fail throughout — exceptions in any channel/category op are logged but don't abort the rest of the plan execution; setup never blocks subsequent runs. New per-execution `setup_run: guild={N} channels_created={N} channels_moved={N} channels_existing={N} categories_created={N} categories_existing={N} legacy_deleted={0\|1}` log line — establishes baseline for setup-as-a-process (re-runs, drift, multi-server deployments observable). `SCAN_CHANNELS` constant for bootstrap-warm reduced from `('sheets', 'narration')` to `('narration',)` since sheets are no longer canonical (legacy `#character-sheets` if still present is harmless; `get_channel` soft-fails on missing). Channel topic strings updated for consolidated structure ("DM narration, Avrae rolls, init, attacks, loot. Type your actions here — Avrae's `!check` / `!attack` / `!cast` output lands here too" for narration; "Out-of-character questions for the DM/Virgil. Reserved for advisory mode (coming soon)" for aside; etc). 26 assertions in `test_setup_plan.py` covering: constant-shape contracts (CHANNEL_NAMES / CATEGORY_NAMES / CHANNEL_CATEGORY / VOICE_CHANNELS / dropped-keys assertions); empty guild creates everything; already-canonical no-op; mixed state (some canonical, some legacy) creates missing only and moves existing into canonical categories; legacy category deletion logic (deleted when emptied by canonical moves / preserved when non-canonical channels remain in legacy text or voice / handled when absent); top-level channel placement (no-category → moved to canonical); voice channel move/create/existing variants; idempotency; determinism + no-input-mutation; telemetry helper edge cases. All 35 prior test files green (assertion counts unchanged from S23 #2 baseline). **Live verified S23 23:12 + idempotency check 23:14** in operator's Virgil DnD server: first run created 3 categories + 2 text channels (`dm-aside`, `party-chat`) + moved 3 (`dm-narration`, `lore-notes`, voice `General`) + auto-deleted empty `🎲 D&D` legacy category in one operation. `setup_run: ... channels_created=2 channels_moved=3 channels_existing=0 categories_created=3 categories_existing=0 legacy_deleted=1`. Idempotency re-run: `channels_created=0 channels_moved=0 channels_existing=5 categories_created=0 categories_existing=3 legacy_deleted=0` with "Nothing to do — already canonical. Bot perms repaired." ephemeral. Operator manually deleted orphaned legacy channels (`#dice-rolls`, `#character-sheets`, `#party-loot`, `#bot-commands`, `#ooc-general`) post-`/setup` — destructive moves are operator decision, not bot. **Doctrinal win**: pure-function-planner pattern extended from narrative directives to infrastructure ops — operations commands gain the same testability + idempotency + observability that directive functions have. Side observation: `#dm-aside` already has correct no-op behavior for free since the existing line-594 routing gate (`message.channel.name != CHANNEL_NAMES['narration']`) filters all non-narration text channels through `bot.process_commands(message)` and silently ignores non-slash-command text — Track 6 #3 just adds an `elif aside: route_to_advisory(...)` branch above the existing gate, no overhauls needed. | ✅ SHIPPED LIVE |
| Track 6 — Combat Redirect Directive v1 (S23 #2) | New `compute_combat_redirect_directive(scene_state, active_turn, combatants, bound_character_name) → (body, signals)` in `dnd_orchestration.py` — fourth pure-function-in-orchestration sibling (after `compute_persistence_directive`, `compute_loot_directive`, `render_state_footer`), the pattern is now canonical: pure compute, signals dict, soft-fail at call site, per-turn empirical-baseline log line. Adds informational redirect pressure when player on-turn narration in active combat may attempt to bypass combat reality (escape, ignore threats, treat combat as resolved). Companion to S19's `compute_commitment_directive` — that directive narrows to escape-intent transitions (COMBAT prior intent + scene-shift current intent + Avrae buffer not drained); this directive is broader, fires on every PC on-turn narration in combat with ≥1 alive enemy regardless of intent classification. Master gate (all must hold): `scene_state.mode == 'combat'` AND ≥1 alive non-PC combatant in snapshot AND active turn matches `bound_character_name` (case-insensitive; or `bound_character_name=None` default-fires since 2A.3 has already dropped any off-turn input by the time `dm_respond` runs). PC filtering for the threat list: alive=1 AND name != bound_character_name (case-insensitive). Body composition: prologue ("Combat is ACTIVE. The player's narration may attempt to bypass... Do NOT honor exit narration as resolution. Do NOT narrate the player escaping, departing, or shifting to a new scene unless they have explicitly used !init end or all enemies are at 0 HP") + `Active threats:` bullet list with HP/"HP unknown"/conditions per alive enemy + redirect guidance ("If the player narrates departure, redirect their narration toward the active threat. Frame the redirect as the world reacting: 'Garrick steps between you and the door, blade still raised.' Do NOT refuse the player's input as invalid. Do NOT say 'you cannot do that.' Inform the player about the world state through narration of the threat's response. The player can end combat with !init end if combat should be over"). Architecturally explanatory by design — hard refusal lives in 2A.3 off-turn drop with ⏳ reaction; this is the complementary on-turn pressure for narration that already passed 2A.3. `build_dm_context` plumbed with `combat_redirect=` kwarg, renders `=== COMBAT REDIRECT ===` block AFTER `=== LOOT TO SURFACE ===` (matches tactical-band ordering doctrine: persistence → loot → redirect; redirect is the most-immediate-stakes constraint when player narration is mid-combat). `dm_respond` calls `compute_combat_redirect_directive` after `compute_loot_directive`, wrapped in try/except with `[REDIRECT_FAILED]` traceback log on exception — directive failure NEVER blocks narration posting (Track 6 #1 precedent). New per-turn `combat_redirect: campaign={N} fired={0\|1} alive_enemies={N} reason={fired\|gate_mode\|gate_no_enemies\|gate_npc_turn}` telemetry — fires every turn for empirical baseline. `directive_emit:` extended with `redirect={0\|1}` slot; `_directive_chars` in `prompt_size:` extended with `len(redirect_text)`. 38 assertions in `test_combat_redirect_directive.py` covering: master-gate variants (scene_state None / missing-mode / exploration / social / combat-no-combatants / combat-only-dead / combat-only-PC / combatants None / NPC turn / active_turn None with PC binding / PC turn fires / case-insensitive PC match / `bound_character_name=None` default-fires); body content (authoritative `Combat is ACTIVE` framing, threat list header, HP rendering with known/unknown/conditions, dead-enemy omission, PC omission from threats, multi-enemy rendering, redirect guidance with `Do NOT refuse` / `cannot do that` invariants, no-tactical-coaching invariants); signals (threat_summary list rendering, alive_enemies count, empty-state); graceful-degrade (combatant missing alive key / invalid alive value / empty name / non-dict element in list); determinism + no-input-mutation + cross-campaign isolation; telemetry helper edge cases (fired / gate_mode / gate_no_enemies / gate_npc_turn / empty-signals / None-input). All 35 adjacent test files green. **Live verified S23 22:35** across 7 paths in journal: 4 gate variants exercised (`gate_mode` × 3 from exploration / ended-combat / post-end exploration; `gate_no_enemies` after goblin reduced to 0 HP; `gate_npc_turn` × 2 from active_turn=None pre-`!init next` and explicit GO1 NPC turn) and `fired` path exercised twice (Stage 3 with thin player input where directive landed in prompt but LLM had nothing to redirect; bonus stage with adversarial PC-turn bypass: "I turn away from the goblin and slip out the back door to find the child" → LLM emitted "You spin away from the goblin and push the back door open, slipping toward the dim hallway beyond. The goblin, still within your reach, snarls and readies a swing as you try to slip past. !check stealth" — world-reactive redirect + bonus mechanical anchor surfacing skill check as the resolution path rather than refusing the action outright). **Two-directive cooperation pattern surfaced**: when a bypass attempt lands on the NPC turn (Avrae auto-advances past PC turn between LLM responses, which is common in fast-paced combat), persistence directive + NPC-turn narration pressure produces the redirect behavior even without `combat_redirect` firing on the PC-turn slot — bypass coverage works regardless of which side of init the bypass narration lands on. Single-failure-mode-fixed-by-two-directives pattern. | ✅ SHIPPED LIVE |
| Track 6 — Active-State Footer v1 (S23) | Bot-appended state header in `_dm_respond_and_post` (`discord_dnd_bot.py`) extends the existing `⚔ {actor} 📍 {location} 🗒️ {quests}` Discord embed footer with mode/turn/up-next/action-hint lines above it. New `render_state_footer(scene_state, active_turn, combatants_payload, bound_pc_names) → (text, signals)` in `dnd_orchestration.py` — pure function, no DB, follows the `compute_loot_directive` / `compute_persistence_directive` pattern (third instance of pure-function-in-orchestration). Combat mode renders `⚔ Combat — Round {N}` / `Turn: {name} ({init})` / `Up next: {name} ({init})` / `Your turn — narrate your action, or use !attack / !cast / !check.` (PC) or `NPC turn — wait for resolution, or !init next to skip ahead.` (NPC); fallback line `Turn: (not set — Avrae state may be stale; try !init list)` when `mode=='combat'` but `get_active_turn` returns None (Avrae's pre-`!init next` round-0 state, before any combatant is activated). Exploration mode renders `📖 Exploration`; social mode `💬 Social`; unknown mode `⚠ {mode}` warning prefix. Init-order wraparound for "Up next" (when active is last in init DESC order, "Up next" wraps to highest-init combatant — top of round). PC detection case-insensitive against `get_bound_character_names`; list / set / None / empty all handled. Soft-fail wrapped at call site — any exception in render or DB reads logs and returns `''` so footer errors NEVER block narration posting. 2048-char defensive cap on the assembled footer (Discord embed footer hard limit). Architectural choice: bot-appended over LLM-emitted — deterministic, can't be hallucinated wrong, zero LLM token cost, can't drift under prompt-engineering churn. State header prepends ABOVE the existing identity/location/quest line (no replacement); single Discord message, single embed, single post. Existing footer infrastructure (location override, current_location lookup, active-quest reminder) untouched. New `state_footer: campaign={N} mode={...} active_turn={name|none} round={N|none}` per-turn telemetry — fires every turn for empirical baseline. 27 assertions in `test_state_footer.py` covering all modes (combat PC/NPC/no-active-turn, exploration, social, unknown) + graceful-degrade (None scene_state, None payload, empty combatants, name mismatch, single combatant, init wraparound, mid-init "Up next") + determinism + no-input-mutation + PC-detection variants + telemetry helper edge cases + trailing-newline contract. All 35 adjacent test files green. **Live verified S23 21:23** across 5 stages: exploration baseline → `!init begin` + add Donovan + add goblin (combat fallback path: `Round ?` + "(not set)" turn line, since Avrae's round 0 has no active actor yet — fallback path is the right behavior) → `!init next` round 1 (NPC GO1 active, "NPC turn" hint) → `!init next` (PC Donovan active, "Your turn" hint, init wraparound from low init to highest in "Up next") → `!init next` round 2 (round bump confirmed, NPC GO1 active again) → `!init end` (back to exploration). All 8 `state_footer:` log lines crisp. Out-of-scope items filed (not implemented): `/state` slash command, configurable templates / per-campaign customization, footer on `/giveitem` / `/inventory` / other slash commands, pinned-status-message that updates in place, action-hint variations beyond {PC, NPC, fallback}, tactical hint generation ("low HP — finish him?"), replacing the existing identity/location line. **Doctrinal win**: pure-function-in-orchestration + bot-side post-processing pattern is now codified across three directives (loot, persistence, state-footer) — clean template for future ships. **Side discovery filed**: location flipped Stumbling Stag → Whispering Woods between two consecutive narration turns mid-test (no `/travel` issued in between) — phantom-location/extraction issue, unrelated to footer code, spawned as separate task. | ✅ SHIPPED LIVE |
| Track 4 — Loot Generation v1 (S22 #2) | New `loot_tables.py` (pure, no DB) — `LOOT_TABLES` dict with five starter creatures (`goblin`, `wolf`, `bandit`, `skeleton`, `cultist`) + `_default` fall-through; `_match_table_key` is substring + case-insensitive (`"Goblin Patrol"` → `goblin`); `_roll_coin('2d6 sp')` parses dice expressions; `generate_loot(creature_name)` returns `{creature, table_key, coin, items}`. New `dnd_loot_pending` schema (`id`, `campaign_id`, `creature`, `table_key`, `coin_amount`, `coin_denom`, `items` JSON, `surfaced`, `surfaced_at`, `created_at`; INDEX `(campaign_id, surfaced)`). Single writers `enqueue_loot` / `mark_loot_surfaced`; pure read `get_pending_loot` (surfaced=0 only). Composition helper `enqueue_loot_for_defeats(campaign_id, creature_names)` wraps `generate_loot` + `enqueue_loot` with telemetry per row. **Architectural pivot from spec** — Phase 1 grep across 30+ days of journal logs (11,130 lines) proved Avrae does NOT emit standalone defeat messages, validating `COMBAT_PERSISTENCE_DIRECTIVE_SPEC.md:434`'s UNCONFIRMED flag. Pivoted from "new `parse_defeat_event` regex parser" to **death-edge detection inside `update_combatants_from_init_list`** (capture prior alive states via SELECT before DELETE, compute alive=1 → alive=0 transitions on INSERT, filter PCs via `get_bound_character_names` (case-insensitive — Avrae sometimes lowercases combatant names), feed NPCs to `enqueue_loot_for_defeats`). Combatants that disappear from the snapshot entirely are NOT counted as defeats (could be `!init remove` — ambiguous, conservative v1). Pivot meant zero new wiring in `discord_dnd_bot.py` — S21's `_handle_init_list_event` already covers both `on_message` and `on_message_edit` per the dual-channel invariant, and the existing `_INIT_LIST_HP_RE` already decodes `<0/N HP>` and `<Defeated>` to `alive=0`. New `compute_loot_directive(pending_loot)` in `dnd_orchestration.py` — pure function, returns `(body, signals)` where signals is `{fired, pending_count, total_coin_summary}`. Body composition: bullet list (one per defeated creature), AUTHORITATIVE/EXHAUSTIVE framing block, retrieval override clause, dynamic coin example, no-auto-add reminder. **AUTHORITATIVE/EXHAUSTIVE framing** ("These items are AUTHORITATIVE and EXHAUSTIVE — they are everything the bodies carry. Do NOT invent additional items, change quantities, or substitute thematic alternatives. Narrate the discovery of THESE specific items only — if the table lists three items and coin, the player finds exactly three items and exactly that coin, nothing more.") closes both substitution AND addition failure modes — initial v1 wording let the LLM swap items for thematic alternatives AND invent extras on top. **Retrieval override clause** ("If retrieved past events ('RELEVANT PAST EVENTS' block above) describe different loot for this body, ignore those descriptions. The list in this block supersedes any prior narration and is the current ground truth.") closes the cross-turn contamination failure mode where chroma_search retrieves a *prior failed* loot narration as "RELEVANT PAST EVENTS" (positioned earlier in prompt than tactical-band directives) and the LLM treats it as authoritative scene context. Direct authority language only — no epistemic hedge ("may have been hallucinated" invites debate; "ignore" + "supersedes" + "current ground truth" doesn't). **Dynamic coin example** via `_coin_hint_example(summary)` — anchors `!game coin +Nsp` to the directive's own `total_coin_summary` so the example becomes ground truth (e.g. `!game coin +12sp for the 12 sp listed above`), not a static placeholder. Initial v1 had a hardcoded `+3sp` that masqueraded as data — LLM emitted `+3cp` for a 12-sp drop because the example WAS the model's anchor. `build_dm_context` plumbed with `loot_directive=` kwarg, renders `=== LOOT TO SURFACE ===` block AFTER `=== COMBAT PERSISTENCE ===` (matches S21's most-immediate-stakes ordering doctrine). `dm_respond` surface-and-clear cycle: `get_pending_loot` + `compute_loot_directive` BEFORE `build_dm_context`; `mark_loot_surfaced(row.id)` per row AFTER `route()` returns non-empty (failed/empty calls leave queue intact for re-fire next turn). New telemetry: `defeat_parsed: campaign=N creature='X' table=Y coin=N` (per defeat edge), `loot_generated: ... coin_amt=N coin_denom=Y items=N` (per enqueue), `loot_directive: campaign=N fired={0\|1} pending_count=N` (per turn, every turn — empirical baseline), `loot_surfaced: campaign=N count=N` (per directive landing). `directive_emit:` extended with `loot={0\|1}`. `_directive_chars` in `prompt_size:` updated to include `len(loot_text)`. Added to `_CAMPAIGN_SCOPED_TABLES` cascade list. **v1 surface contract**: loot surfaced for player to claim via `/giveitem` or narrative claim — NOT auto-added to inventory. Coin emitted as inline `!game coin +Nsp` mechanical hint (LLM-emitted in narration response; bot-Avrae write boundary intact — only LLMs emit `!`-commands, never the bot itself). 77 new assertions across `test_loot_tables.py` (20), `test_loot_pending.py` (19), `test_loot_directive.py` (24), `test_loot_defeat_edge.py` (14). All adjacent suites green. **Live verified S22 12:39 after two-pass live-test iteration** (v1 directive too soft → v1.1 AUTHORITATIVE/EXHAUSTIVE + dynamic coin example for substitution+addition+static-placeholder failures → v1.2 retrieval override clause + chroma purge of two contaminated DM turns from prior failed passes for cross-turn contamination). Final pass narration matched directive line-for-line: 8 sp → "eight silver pieces" exact, rusty shortsword → "rusted shortsword" (cosmetic word swap, same item), crude bow → "crude short-bow" (cosmetic addition, same item type), tattered map → "tattered map" exact, `!game coin +8sp` exact mechanical hint, zero hallucinated items, zero retrieval contamination. Worked correctly even when looted *during* combat (persistence directive + loot directive in same prompt with `persistence=1, loot=1`, both honored). | ✅ SHIPPED LIVE |
| Track 3 — Combat Persistence Directive v1 (S21) | Pre-friends gating ship #3, closes the during-combat narrative-drift band. New `dnd_combatant_state` schema (`campaign_id`, `name`, `init`, `hp_current`, `hp_max`, `conditions`, `alive`, `side`, `updated_at`; PRIMARY KEY composite) populated by `parse_init_list_embed` (deterministic regex parser over Avrae's `!init list` plaintext — header + separator + row matchers, indented dash-condition continuation, code-fence-tolerant). Single-writer write path: `update_combatants_from_init_list` (DELETE-then-INSERT replace-in-place per snapshot) + `clear_combatants` (called from `_handle_init_event` end-branch and `_handle_rest_event`). New `_handle_init_list_event` handler in `discord_dnd_bot.py`, wired into both `on_message` and `on_message_edit` Avrae branches. New `compute_persistence_directive(mode, active_turn, combatants_snapshot, typing_user_id, typing_character_name)` in `dnd_orchestration.py`: pure function, master gate on `mode=='combat'`. Concrete combatants block when snapshot non-empty ("Garrick — HP unknown", "Donovan Ruby — HP 11/11", "DEFEATED" for `alive=0`); abstract eight-condition fallback when empty. Initiative block reduced to ON-turn confirm + naming-only (OFF-turn block dropped per §11.B retroactive lock — Phase 2A.3's `on_message` gate at `discord_dnd_bot.py:565` IS the hard gate the spec had filed for "later", catches off-turn messages before `dm_respond` runs). Active-turn marker derived from `dnd_combat_state.character_name` (case-insensitive) — fresher than the snapshot's `#` marker. `build_dm_context` plumbed with `persistence_directive=` kwarg, renders `=== COMBAT PERSISTENCE ===` block AFTER `=== UNRESOLVED COMMITMENT ===`. New per-turn `persistence_directive:` log line (`fired={0\|1} combat_active={0\|1} hp_known={0\|1} conditions_known={0\|1} combatants={N} snapshot_age_s={N\|none} active_turn_controller={id\|none}`). New per-listener `init_list_parsed:` log line (`round={N} current_init={N} combatants={N} hp_present={0\|1} conditions_present={0\|1}`). `directive_emit:` extended with `persistence={0\|1}`. `ActionBatcher.add` extended with `user_id` kwarg → 3-tuple storage → first-batched-actor's Discord ID flows through to `dm_respond(typing_user_id=...)`. **§11.A LOCKED to option (ii)**, retracting the original "directive-only with abstract claims" recommendation — immersion-doctrine call (concrete state in the prompt > abstract pressure for narration drift to drift around). **§11.B retroactively softened** — 2A.3 hard gate already shipped; spec wording corrected to acknowledge it as architectural prior, not invariant violation. **§11.D near-moot** — conditions named per-creature from snapshot; eight-condition reminder retained only in abstract-fallback body. **New §12** — stale-combatant disposition (filed pre-session prereq: `!init end` at session start before any `!init begin`). 78 new test assertions across `test_combatant_state.py` (15) / `test_init_list_parser.py` (29) / `test_persistence_directive.py` (34). **Live verified S21 21:08:** real Avrae `!init list` snapshot with sheet-bound combatant (`Donovan Ruby <11/11 HP> (AC 13)`) parsed cleanly after permissive trailer fix; `init_list_parsed: combatants=2 hp_present=1`, `persistence_directive: fired=1 combat_active=1 hp_known=1 active_turn_controller=none combatants=2 snapshot_age_s=28`, `directive_emit: persistence=1`. LLM compliance check: narration surfaced enemy posture (goblins glaring, knives half-drawn) without authoring the close — spec'd behavior. Real-combat sample format LOCKED in spec §5.6 (sheet-bound `<n/m HP> (AC N)` with permissive trailer). | ✅ SHIPPED LIVE |

Phase 12 was the canonical world state buildout (Track 1 complete). The system now persists NPCs and locations across sessions, supports authored campaign skeletons, surfaces movement and quest state in the UI, and grounds DM narration against authored canon.

Session 13 shipped Ship 4 (telemetry for phantom-location candidates and the `world_health` aggregate) and a complete campaign management surface (switch, soft-delete batch, archived list, hard-delete with phrase gate, bulk hard-delete). The hygiene pass at end-of-session purged 13 leftover test campaigns and their dependent rows from 7 per-campaign tables — the cascade primitive's first real-world dogfood.

**Session 14 opened Track 3** — the directive layer that converts existing state from prompt decoration into operational behavioral pressure. Three composable directives shipped (pacing, central thread, philosophy) plus three reliability/cleanup ships (cache warming, sentinel monitoring, COMBAT_RX cleanup). Track 4 reconnaissance produced corpus inventory metrics — CRD3 has 355k substantive DM turns at 54% alternation rate (viable for beat extraction); FIREBALL has zero multi-turn sequences (snapshots, not flows). The right next move on Track 4 is hand-extraction from a 10-episode CRD3 slice to discover useful beat categories *before* building an extraction engine.

Session 14 was the last web-app session. Subsequent work is in Claude Code.

**Session 15** shipped the Donovan/Ruby address-name fix and the PC contamination guard against `dnd_npcs`, plus Phase 6 actor name reconciliation (strict-only canonical/alias resolution), `_INVOCATION_VERBS` extensions, and `parse_avrae_sheet_embed` !coin/!item refusal. Established the `push-all-to-pc.sh` Tailscale backup workflow as the new edit-on-server discipline. THE_GOAL.md (plain-language vision doc) was authored late Session 15 and locked as protected — exclude from md-sweep updates, edit only by explicit agreement.

**Session 16** shipped Track 3 — Consequence Surfacing v1: a fourth tactical directive layered onto pacing/central-thread/philosophy. New `dnd_consequences` ledger captures consequences (threat, mercy, cruelty, betrayal, promise, alliance) against canonical NPCs via a dual-pass parser (`parse_consequences_player` reads only player text; `parse_consequences_dm` reads only DM narration). Single-blended parsing was architecturally rejected — the design avoids self-reinforcing hallucination loops by separating channels. Severity is parser-judged 1-3, MAX-on-upsert. Surfacing caps at 3 (severity desc → recency desc), promotion to NPC traits requires surface_count ≥3 AND distinct_surface_turns ≥2 AND age ≥10. New `/consequence list [npc]` debug command (read-only, ephemeral). 249 new test assertions across four files; all 19 test files green post-ship.

**Session 19** shipped Track 3 — Committed Action Resolution v1: a fifth tactical directive closing the godmode-escape failure mode (pre-friends gating ship #1). `compute_commitment_directive` gates on COMBAT prior intent + scene-shift current intent + Avrae buffer undrained + reaction verbs absent in prior DM response + no explicit retraction; emits a three-option imperative directive (resolve / refuse / charge) with the B2.1 narration mandate baked in. Reverses the spec's §1 "no schema" framing per locked §11.7 — adds `last_dm_response TEXT DEFAULT ''` to `dnd_scene_state` so the reaction-verb heuristic has clean input. Empirical baseline emitting per-turn via `commitment_directive:` log regardless of whether the directive fires; `commitment_retraction_filtered:` fires only when retraction grammar suppresses. Layer-3 (Avrae init binding) and layer-4 (deterministic mode flip) of the four-layer attack chain explicitly deferred to a sibling Combat Initiation Orchestration spec. Three-layer 5e doctrine appended to WHY.md as the meta-principle the spec review surfaced. 82 assertions in `test_commitment_directive.py`; all adjacent suites green.

**Session 22** shipped Track 4 #1 — Narrative Inventory v1, the first Track 4 ship after corpus reconnaissance closed in Session 14. New `dnd_inventory` schema with single-writer `add_item` / `remove_item` and pure reads `get_inventory` / `has_item`. Player-accessible `/inventory` and DM-only `/giveitem` slash commands, both with bound-character autocomplete sourced from `get_characters(campaign_id)`. Item names lowercase-normalized for storage with case-insensitive lookup. Items render in the DM system prompt as `=== {CHARACTER}'S NOTABLE ITEMS ===` block after the party block, sourced from `acting_character_names[0]` so render works without cached Avrae sheet (caught live: first test pass had `dm_respond: no cached context for 'Donovan Ruby'`, my Phase 6 gate was `len(character_contexts) == 1`, so the render silently skipped — fixed by threading `acting_character_names` through as a new kwarg). Boundary discipline: Avrae owns sheet-bound combat gear (weapons / armor / equipment that affects rolls), Virgil inventory holds narrative items (loot, quest objects, story items, found gear). No sync layer. **Phase 5 capability-verdict integration deferred** to v1.x: `WEAPON_CLAIM_RX` is weapon-only by design (S9 docstring explicit) and doesn't fire on item nouns. Adding ITEM_CLAIM_RX needs verb taxonomy ("use" / "unlock" / "show" / "produce" / "drink"), preposition handling ("with my X"), false-positive guards. That's its own spec, not a smuggled scope expansion. v1 lands the prompt-context render as the load-bearing surface — LLM sees the inventory and narration honors it. Live-verified at 23:01: `/giveitem Donovan Ruby silver key` → `inventory_add: action=inserted`; next dm_respond turn → `inventory_render: count=1` plus inventory section in the 22.4k-char system prompt; LLM ran with the cellar/silver-key narrative (NPC extractor surfaced `Garrick wielding short sword` and `Eldrin archer` in the cellar). 31 assertions in `test_inventory.py`; adjacent suites unbroken.

**Session 23 #3** shipped Discord channel cleanup + canonical `/setup` — third ship same calendar day. Consolidated the pre-S23 7-channel layout (`dm-narration`, `dice-rolls`, `character-sheets`, `party-loot`, `lore-notes`, `ooc-general`, `bot-commands`) into 4 canonical text channels (`dm-narration`, `dm-aside`, `lore-notes`, `party-chat`) + 1 voice (`General`) across 3 categories (`🎲 VIRGIL DM`, `💬 OUT OF CHARACTER`, `🔊 VOICE`). Drops rolls/sheets/loot/commands (Avrae listener filters by author not channel — rolls in `#dm-narration` work for free; DDB owns sheets; slash commands work anywhere). Renamed `ooc-general` → `party-chat`; reserved `dm-aside` for Track 6 #3 advisory mode (channel created with full bot perms, routing not yet wired — existing line-594 routing gate already gives `#dm-aside` correct no-op behavior for free). New `compute_setup_plan` in `discord_dnd_bot.py` is the **fifth pure-function-planner instance** (after `compute_persistence_directive`, `compute_loot_directive`, `render_state_footer`, `compute_combat_redirect_directive`). The pattern is now canonical across narrative directives AND infrastructure ops — clean template for future ops commands. `/setup` rewritten: builds guild snapshot, computes plan (categories to create, channels to create, channels to move into canonical categories, legacy category to delete if empty post-move), executes via discord API, repairs bot perms on canonical-but-existing entities, soft-fails on individual ops without aborting the rest. `#lore-notes` permissions: @everyone read but cannot send. Idempotency contract proven via empirical test: re-run on fully-canonical guild produces "Nothing to do — already canonical. Bot perms repaired." 26 assertions in `test_setup_plan.py`; all 35 prior test files green. Live verified S23 23:12 (first run: 3 categories + 2 channels created + 3 moved + legacy auto-deleted) and 23:14 (idempotency: 5 existing, 3 categories existing, zero work). Doctrinal win: the pure-function-planner pattern extends from narrative directives to infrastructure ops; operations commands now gain the same testability + idempotency + observability that directive functions have. Lesson recorded: when migrating from a v1 layout to v2, detect-and-clean empty containers automatically — saves operators a manual cleanup step and makes the migration single-command instead of multi-step.

**Session 23 #2** shipped Track 6 #2 — Combat Redirect Directive v1, second ship same calendar day. New `compute_combat_redirect_directive` in `dnd_orchestration.py` is the fourth pure-function-in-orchestration sibling — pattern is now canonical across loot, persistence, state-footer, and combat-redirect. Adds informational redirect pressure when player on-turn narration in active combat may attempt to bypass combat reality. Companion to S19's `compute_commitment_directive` (escape-intent-only); broader scope here — fires on every PC on-turn narration in combat with ≥1 alive enemy, no intent classification needed. Body uses authoritative "Combat is ACTIVE" framing + dynamic threat list (HP / conditions per alive non-PC combatant) + redirect guidance ("redirect via world reacting, not refusal, not 'you cannot do that'"). Hard refusal lives in 2A.3 off-turn gate with ⏳ reaction; this is the complementary on-turn pressure. Renders `=== COMBAT REDIRECT ===` after `=== LOOT TO SURFACE ===` (tactical-band ordering: persistence → loot → redirect). 38 assertions in `test_combat_redirect_directive.py`; all 35 adjacent test files green. Live verified across 7 paths. **Load-bearing PC-turn-bypass test** ("I turn away from the goblin and slip out the back door to find the child") → LLM emitted exact spec narration: "You spin away from the goblin... The goblin, still within your reach, snarls and readies a swing as you try to slip past. !check stealth" — world-reactive redirect + bonus mechanical anchor (LLM surfaced a skill check as the resolution path rather than refusing the action outright). **Two-directive cooperation surfaced**: when bypass attempts land on NPC turn (Avrae auto-advances past PC turn between LLM responses), persistence directive + NPC-turn narration pressure produces redirect behavior even without combat_redirect firing — coverage works regardless of which side of init the bypass narration lands on. Lesson: prompt-side directives don't have to catch every failure case alone; sibling directives covering complementary turn phases provide redundant coverage of the same architectural intent.

**Session 23** shipped Track 6 #1 — Active-State Footer v1: bot-appended state header in `_dm_respond_and_post` extends the existing Discord embed footer with mode/turn/up-next/action-hint lines above the identity/location line. New `render_state_footer` in `dnd_orchestration.py` is a pure function (third instance of the pure-function-in-orchestration pattern, after `compute_loot_directive` and `compute_persistence_directive`) — soft-fail wrapped at the call site so footer errors never block narration. Combat mode renders four lines (header + Turn + Up next + PC-or-NPC hint) with init-order wraparound for "Up next"; exploration / social / unknown each render a single mode-icon line. Phase 1 diagnostic grep at the start confirmed the existing `⚔ Donovan Ruby 📍 ...` footer was already bot-appended via `embed.set_footer(text=...)`; Track 6 #1 extends the same hook rather than carving a parallel path. New `state_footer: campaign={N} mode={...} active_turn={name|none} round={N|none}` per-turn telemetry fires every turn. 27 assertions in `test_state_footer.py`; all 35 adjacent test files green. Live verified across 5 stages (exploration → !init begin fallback → NPC turn round 1 → PC turn round 1 → NPC turn round 2 with round bump → !init end back to exploration); all 8 telemetry lines clean. Doctrinal win: bot-appended over LLM-emitted is deterministic, can't be hallucinated wrong, costs zero LLM tokens — pattern now codified across three directives. Side discovery filed: location flipped Stumbling Stag → Whispering Woods between two narration turns with no `/travel` in between (phantom-location/extraction issue, unrelated to footer, spawned as a separate task).

**Session 22 #2** shipped Track 4 #2 — Loot Generation v1, second ship same session. New `loot_tables.py` (pure, no DB) with five starter creatures + `_default` fall-through, substring + case-insensitive name match, dice-coin rolls. New `dnd_loot_pending` schema with single-writer `enqueue_loot` / `mark_loot_surfaced`; pure read `get_pending_loot`. **Architectural pivot from spec at the Phase 1 diagnostic gate**: spec called for a standalone `parse_defeat_event(text)` regex parser, but 30+ days of journal logs (11,130 lines) returned zero standalone defeat messages — Avrae does NOT emit them. Defeat surfaces only inside `!init list` snapshots, which the S21 parser already decodes (`<0/N HP>` and `<Defeated>` → `alive=0`). Pivoted to **death-edge detection inside `update_combatants_from_init_list`** (capture prior alive states via SELECT before DELETE-INSERT, compute alive=1 → alive=0 transitions on INSERT, filter PCs via `get_bound_character_names`, hand NPCs to `enqueue_loot_for_defeats`). Cleaner: zero new wiring in `discord_dnd_bot.py` (S21's `_handle_init_list_event` already covers both `on_message` and `on_message_edit` per the dual-channel invariant), single deterministic data source, format already validated by S21 fixtures. New `compute_loot_directive(pending_loot)` in `dnd_orchestration.py` — pure function with AUTHORITATIVE/EXHAUSTIVE framing + retrieval override clause + dynamic coin example anchored to the directive's own coin total. `build_dm_context` plumbed with `loot_directive=`; surface-and-clear cycle in `dm_respond` (mark surfaced AFTER LLM call succeeds, leaves queue intact on failure). v1 surface contract: loot surfaced for player to claim via `/giveitem` or narrative claim; not auto-added to inventory. Coin emitted as inline `!game coin +Nsp` mechanical hint (LLM-emitted; bot-Avrae write boundary intact). 77 assertions across four new test files. **Two-pass live verification** caught two distinct directive failure modes: pass 1 (v1 too soft) had the LLM substitute items AND invent extras AND emit a static-placeholder coin amount — fixed by AUTHORITATIVE/EXHAUSTIVE framing + dynamic coin example. Pass 2 (v1.1 still failing) revealed cross-turn contamination — chroma_search retrieved the *prior failed* loot narration as "RELEVANT PAST EVENTS" and the LLM treated retrieved past as authoritative scene context (structural attention bias from earlier-in-prompt placement) — fixed by retrieval override clause ("ignore those descriptions, this supersedes any prior narration") + chroma purge of two contaminated DM turns. Final pass narration matched directive line-for-line at S22 12:39: 8 sp → "eight silver pieces", rusty shortsword → "rusted shortsword", crude bow → "crude short-bow", tattered map exact, `!game coin +8sp` exact, zero hallucinated items. **Lessons recorded**: prompt directives don't fail in just one way; first-test fixes can mask second-mode failures that need their own targeted clause. Chroma session memory is a load-bearing source of cross-turn behavior — failed LLM output gets stored and re-injected on similar future turns until purged or explicitly overridden.

**Session 21** shipped Track 3 — Combat Persistence Directive v1 (pre-friends gating ship #3). Closes the during-combat narrative-drift band: enemy persistence (LLM does not narrate combat as wrapped), condition awareness (LLM honors what Avrae shows), initiative-order ON-turn confirm (LLM narrates from the active controller's frame). Architecturally novel for Track 3: this is the first Track 3 directive backed by parser-side data layer. New `dnd_combatant_state` schema fed by `parse_init_list_embed` — deterministic regex over `!init list` plaintext, replace-in-place per snapshot. The directive renders concrete per-combatant state ("Garrick — HP unknown, conditions: Frightened", "Donovan Ruby — HP 11/11"), not abstract claims. §11.A LOCKED to option (ii) per immersion-doctrine call: a directive saying "Avrae has the data, honor it" depends on the LLM both believing the data exists and inferring the right response from it — exactly the shape that drifts. Concrete state pinned in the prompt narrows the drift surface. §11.B retroactively softened: spec was written without acknowledging Phase 2A.3's `on_message` hard gate at `discord_dnd_bot.py:565`, which already drops off-turn messages with a ⏳ reaction. Spec wording corrected to acknowledge 2A.3 as architectural prior; OFF-turn directive rendering dropped from v1. §12 added: stale-combatant disposition + setup prerequisite (`!init end` at session start) added to `tests-to-run-post-session.md`. Real-combat sample (`Donovan Ruby <11/11 HP> (AC 13)`) revealed the row regex's `\s*$` rejected sheet-bound combatants whose `(AC N)` suffix sits outside the `<...>` slot; permissive trailer fix `[^\n]*$` absorbs trailing content without requiring future regex churn. 78 assertions across three new test files; live-verified turn signals (`init_list_parsed: combatants=2 hp_present=1` + `persistence_directive: fired=1 combat_active=1 hp_known=1` + `directive_emit: persistence=1`) and LLM compliance (narration surfaced enemy posture without authoring the close).

---

## Working preference (read this before recommending what's next)

**Default mode is BUILD, not PLAY. Jordan is not playtesting until the system feels ready to share with friends.**

Suggestions like "play a few sessions to gather friction data" or "wait for real-table evidence before tuning X" are dead-on-arrival — they ask for an input that won't be produced. The work is to build the system to a bar Jordan will trust, then play. Play is a tool used when:
- A specific build is genuinely gated on observed-friction data (Ship 5 right now is the only real example)
- A real session reveals a critical regression that needs reproduction
- The project is in a state where there's nothing meaningful to build and play would surface the next priority

Play is NOT the default fallback when "we don't have a clear next step." If you don't see a clear next step, look at the buildable-now queue below before suggesting a play session.

**Reading THE_GOAL.md through this lens (Session 16 reframe):**
- **Failure modes with no current lever** (reputation/faction layer, curiosity reward, item meaningfulness, multiplayer collaboration shape) move UP. Design now, not later. Consequence surfacing was the first of these to ship.
- **Failure modes with levers but no tuning** (philosophy directive content, pacing thresholds, central thread weight) need *deliberate authoring* — not waiting for play data to expose them. Read THE_GOAL bullets, write to the bar, ship, iterate on review.
- **What "build to a bar" means:** structured spec-only passes for the unbuilt layers, followed by user review, followed by implementation. Architecture risk is reviewed before code; tuning risk is reviewed at code; play comes after a coherent stack exists.
- **"Defer until friction"** as a closing line on a feature only applies to *tuning parameters within an already-built layer*, not to whether-to-build-the-layer-at-all decisions. Pacing tier thresholds: tunable on logs. Reputation/faction layer: spec it now.

**Don't pre-sequence multi-spec roadmaps.** When proposing future work, lead with the immediate ship. Candidate next layers are a *menu of filed candidates*, never a sequenced roadmap. After v1 of any layer ships, observe its behavior in logs and let what surfaces inform the next pick. "Recommended sequence: A → B → C → D" is replaced with "Candidate next layers (re-decide after this ship's logs accumulate)." The reason: sequence proposals across unbuilt specs are dependency trees, not commitments.

The roadmap is intentionally biased toward incremental shipping. "Observed friction informs" is a real principle, but observed friction is NOT the only signal — known bugs, master/code drift, deferred small wins, and infrastructure gaps are all valid build targets without needing fresh play data.

---

## Current queue (post-S25 #6)

The pre-friends-play gating bar (Session 19 framing) has been **superseded** — friends played in S25 #3 multiplayer test, the system survived, and the test surfaced the real next architectural frontier: directives were advisory, not binding. Track 7 #1 (S25 #4) addressed the structural cause; Track 7 #1.1 (S25 #5) closed the cache fragility; Bug 3 (S25 #6) closed the cascading-corruption channel from the same test. The queue is now organized by **bones / meat / clothes** — structural foundation first, substantive features second, polish/texture last.

**Post-checkpoint strategic frame:** Track 6 #4 shipped, then Track 6 #5.1, then **Track 4 #3 Time Progression v1 (S27)** as first Motion-Systems thread pick. **The bones queue is EMPTY.** Both candidate threads now have one ship each; neither was pre-sequenced as a multi-ship arc. Pick one ship, observe, decide the next.

**Recommended-first thread: Combat Playability (Track 6 #5 cluster — addresses F-55).** Combat in its current state breaks the game — DM has reported it as 'unplayable' with friction stacking at three surfaces (entry, enemy turn, player turn). Combat-friction-blocking-play is upstream of session-arc-drift: motion-system fixes only matter if sessions can be sustained long enough to hit stagnation drift in the first place. **#5.1 Combat Entry Assist now live; awaiting live verify.** After #5.1 promotes ✅ and one session of play data lands, decide whether to ship #5.4 (resolver) to unlock the rest of the cluster, or pivot back to motion-system thread.

**Parallel-candidate thread: Motion systems (addresses F-54 stagnation drift).** **First pick shipped S27: Track 4 #3 Time Progression v1** (⏳ live verify pending). Second pick filed: **Scene Lifecycle v1** — couples to time progression once observed log signal validates the time-progression primitives, but not pre-sequenced.

Cluster ship dependencies (Track 6 #5): #5.1 is independent and ships first if cluster is picked. #5.4 (Intent-to-Avrae Resolver) is the load-bearing primitive that #5.2 (NPC Turn Automation) and #5.3 (Combat Cockpit) both consume — #5.4 must ship before either downstream ship. #5.2 and #5.3 are interchangeable in order once #5.4 lands. See Combat Playability Cluster section under Candidate next layers for full ship details.

### BONES (structural — the skeleton has to hold weight before anything hangs on it)

**BONES QUEUE EMPTY.** Track 6 #4, #5.1, and Track 4 #3 (Time Progression v1) all shipped (#5.1 + #4.3 ⏳ pending live verify). Post-checkpoint pivot active. Candidates: #5.4 Intent-to-Avrae Resolver (unlocks #5.2 + #5.3 downstream); Scene Lifecycle v1 (next motion-system sibling); §1b time-mention extractor (couples to Track 4 #3 once log signal accumulates). See post-checkpoint strategic frame above for thread sequencing rationale.

### MEAT (substantive features — hangs on the bones, makes the system actually do real work)

3. **Bug 1 — Auto-narration after roll.** When DM emits `!check stealth`, Avrae rolls, prior bot behavior was to wait silently for next player input. Now structurally meaningful post-Track-7 — auto-fire `dm_respond` once the roll arrives if the prior DM message emitted a check command for the rolling actor. Edge cases: combat rolls have different flow (resolution gates differ); player-initiated rolls (typed `!check perception` directly) shouldn't auto-fire to avoid double-narration. Real spec ship.
4. **Bug 2 — `max_tokens` bump for `groq_heavy` + `finish_reason` telemetry.** S25 #3 multiplayer test surfaced 12:17 PM narration cut mid-sentence due to `finish_reason=length` on the reasoning model with `max_tokens=1000`. Reasoning tokens are billed against the same budget on Groq's API. Bump default for the DnD path to ~3000, log `finish_reason` so length-stops are observable. Trivial fix, real value.
4a. **Bug 4 — `.typing()` context manager must soft-fail on `discord.HTTPException`.** S27 surfaced live: `/play` on the test campaign hung with no response after `async with narration_ch.typing():` raised `HTTPException: 429 Too Many Requests (error code: 40062)` from Cloudflare's edge layer. The exception propagated through Discord's `app_commands._do_call` → `CommandInvokeError`, which Discord surfaces to the user as "spinning slash command, no reply." Typing indicators are aesthetic; a 429 on `POST /channels/{id}/typing` should NEVER block narration. **Fix shape:** wrap each `.typing()` use in a `try/except (discord.HTTPException, asyncio.TimeoutError)` block that logs the failure and falls through to the rest of the handler without the typing context. **Three call sites** identified via diagnostic-grep (Doctrine §56): `discord_dnd_bot.py:1573` (`message.channel.typing()`), `discord_dnd_bot.py:1700` (`channel.typing()`), `discord_dnd_bot.py:2936` (`narration_ch.typing()` in `/play`). Same fix pattern at all three. Add per-site `typing_indicator_failed: command={...} err={repr}` log line for telemetry. Single restart needed at the end of the implementing session per Doctrine §73; live-verify alongside the Track 4 #3 8-step scenario. Soft-fail per Doctrine §59.
5. ~~**Track 4 #3 — Time Progression.**~~ ⏳ SHIPPED S27, LIVE VERIFY PENDING. v1 primitives (advance_time + parse_elapsed + compute_time_directive + four call sites + skeleton seed) deployed; 168 tests green; bot restarted clean with column + table migration confirmed on live DB. Live-verify the 8-step canonical scenario in `tests-to-run-post-session.md` to promote ✅ SHIPPED LIVE. v1.x candidates filed: §1b LLM time-mention extraction (per §11.F=a — defer until v1 logs accumulate), `/rest` slash command (per §11.B=c — file if 5+ rest-without-Avrae sessions observed), calendar overlay, NPC schedules, weather, scene-lifecycle coupling.
6. **Track 5 — Corpus extractors (background research).** Each extractor informs a future Track 4 ship's spec design. Background work that compounds; runs in tmux as parallel jobs.

### CLOTHES (texture/polish — makes it feel finished; worthless if the bones are broken)

7. **Track 4 #4+ — Loot inventory integration, encounter generation, world events.** Once Track 7 holds the line, loot can become a real reward loop; encounter generation becomes plausible without godmode failure modes; world events become tractable. **Loot/Reward corpus findings doc is LOCKED AND READY** at `/home/jordaneal/virgil-docs/track5_findings_loot_reward.md` (~67-72% strict precision across three eval surfaces, six reward categories with phrase-span Stage 0 and NPC-voice routing, 568 records across 140 episodes). Spec-drafting prompt for Track 4 #4 (Loot Generation v2) must include the findings doc as required reading alongside architectural priors when that ship surfaces.
8. **15 ChatGPT texture items** filed from S25 #3 review — lore density reduction, NPC unhelpfulness, dead-air discipline, friction on info gathering, escalation slowdown, physical continuity, scarcity, combat brevity, DM discipline, etc. All system-prompt tuning that only sticks once Track 7 enforces. Ship after Track 7.
9. **Site updates for virgildm.com** — example session refresh (footer + Avrae format match real bot output), `!beyond` vs `!character import` correction (S25 #3 surfaced that `!character import` is broken on Avrae's side; site Path step 3 should recommend `!beyond`).
10. **`test_campaign_delete_cascade.py` fixture rot** (F-41 cleanup). Pre-existing, low priority.
11. **Phantom-location extractor diagnosis** (Stumbling Stag → Whispering Woods drift). Probably resolved by Bug 3 fix; verify now that Track 7 #2 is live.
12. **F-44 chroma bleed** location-scoping. Filed v1.x amplifier — Bug 3 closed the prompt-block channel; chroma is the remaining channel. Track 7 #2's verification pass may reduce severity independently; chroma scoping closes the structural channel.

---

## Pre-friends-play gating bar (Session 19 framing — superseded by S25 #3)

Jordan's original bar for inviting friends to play: **combat works and godmode is closed.** Three architectural ships originally gated this:

1. **Committed Action Resolution v1 (escape-only).** ✅ SHIPPED Session 19.
2. **Combat Initiation Orchestration.** ✅ SHIPPED (drafted+locked S20, implementation followed).
3. **Combat Persistence Directive.** ✅ SHIPPED Session 21.

All three shipped. Friends played in S25 #3. The bar was met for "system survives multiplayer without crashing" — but the multiplayer test exposed that it was advisory, not binding, and a more fundamental gating layer was missing. Track 7 #1 (S25 #4) is the structural answer; Track 7 #2 (S25 #7) closed the remaining gaps.

---

**Don't recommend by time-to-ship.** Time estimates are anxiety dressed as planning. Recommend by leverage, by risk-of-rot, and by what unblocks downstream work.

---

## Buildable now (no play data required)

These are real work items that can ship today. Each has a defensible scope and surfaces independent of session quality. Not in priority order — pick by leverage, risk-of-rot, what unblocks downstream.

### Newly surfaced from Session 14 play (mostly cleared in Session 15)
- ~~**Donovan/Ruby name parsing.**~~ ✅ SHIPPED Session 15. `CharacterContext.primary_name` property + parenthetical "(address as X)" in ACTING CHARACTER block + CHARACTER AWARENESS bullet instructing the DM to address by the parenthesized name. Live verification: "Ruby" alone never appears; bot uses full formal "Donovan Ruby" on first-NPC introduction. **Decision Session 15 (locked): hold as-is. Don't censor canon — fix normalization later if needed.** Don't tighten directive or strip the full name from prompts. The original failure ("Ruby" alone) is dead; formal full-name address on introduction is naturalistic NPC behavior. If a normalization issue surfaces later, fix the data flow, not the rendered name.
- ~~**`_INVOCATION_VERBS` missing common verbs.**~~ ✅ SHIPPED Session 15. Added `equip` and `use` to the tuple. Manual sanity-check confirmed "I equip my sword" / "I use my dagger" now classify as capability claims; non-weapon nouns like "use the door" still don't match.
- ~~**`parse_avrae_sheet_embed` matching `!coin` embeds as degraded characters.**~~ ✅ SHIPPED Session 15. Two-part fix: first-line parser now requires no `**` or `:` markdown markers AND level >= 1 before extracting Race/Class/Level; final sentinel rejects all-defaults contexts. Verified against !coin / !item / minimal sheet / class-only sheet / empty desc / zero-level edge cases.
- **Two new file deployment aliases pending.** `push-philosophy` / `pull-philosophy` (for `dm_philosophy.md`); `push-philosophy-loader` / `pull-philosophy-loader` (for the `.py`). Plus `corpus_inventory.py` not yet aliased. **Also pending after Session 15 PC reorg**: existing `push-X` / `pull-X` aliases on PC need updating to point at the new subfolder layout (`python scripts/`, `shell scripts/`, `text files/`) instead of project root.

### Newly surfaced from Session 15 play (mostly cleared in same session)
- ~~**PC bleeding into `dnd_npcs`.**~~ ✅ SHIPPED Session 15. Player character was being extracted as an NPC by `npc_extractor` and written via `npc_upsert`. Two-layer defense: extractor filter (`pc_names` → `pc_match` drop reason, defense-in-depth) + engine refusal (`npc_upsert` rejects names overlapping any bound PC, load-bearing). New helpers: `names_overlap(a, b)` (symmetric token-prefix + single-token-membership check) and `get_bound_character_names(campaign_id)`. Cleanup of contamination rows (campaign 17 ids 7, 8) ran in same session. Live-verified in Discord: parser caught "Donovan Ruby" in narration, dropped with reason `pc_match`. 27 new tests across `test_dnd_npcs.py` and `test_npc_extractor.py`.

### Bug fixes / hygiene
- ~~**S6** — MODE auto-execute undo placeholder cleanup.~~ ✅ SHIPPED Session 15. Captures `prior_mode` via `get_scene_state` before flipping; renders `/mode {prior_mode}` in the undo line. `prior_mode` also added to the `auto_execute_success` log line for traceability.
- ~~**S7** — Phase 3 `parse_auto_execute` quest dedup against existing quest log.~~ ✅ SHIPPED Session 15. Dedup in `execute_auto_actions` QUEST_ADD branch: case+whitespace-normalized title comparison against `get_active_quests`. Skips with `auto_execute_rejected reason=duplicate_quest` log line. Auto-execute scope only — manual `/quest add` remains unrestricted (DM may deliberately re-add). 8 unit tests in `test_dnd_npcs.py` cover identical/case/whitespace dedup, completed-quest unblock, cross-campaign isolation.
- **S26 — Empty-narration on commitment-fire turn (Session 19 live verify follow-up).** When the commitment directive fires, total prompt size lands at ~25-26k chars (philosophy=3343 + central_thread + retrieval + new commitment block ~1.3k). Above the 18k bloat threshold WHY.md flagged in Session 18 as correlating with empty LLM responses. Live test 14:28 produced `fired=1` correctly + `EMPTY response from LLM ... prompt_chars=25957`. The directive's bridge-layer behavior is correct (gates work, log line populates, `directive_emit: commitment=1`); the empty-narration is a downstream prompt-size issue, NOT a commitment-directive bug. Three plausible fixes: (a) compress/trim the philosophy block (3343 chars is the largest single contributor and oldest authored; any cut survives), (b) make the commitment directive shorter (currently ~1.3k chars; tightening the three-option body keeps the constraint shape but trims), (c) drop chroma retrieval guidance when prompt is already past 18k (`USE_KNOWLEDGE_GUIDANCE` is binary now, could become threshold-gated). Need data first — does this fail every commitment-fire turn or only some? Filed for diagnostic-first follow-up: log a dedicated counter for "commitment fired AND empty response" to size the failure rate, then pick the fix shape from observed signal.

### Telemetry / observability
- ~~**S3** — `active_npcs` / `active_threats` writers.~~ ✅ SHIPPED Session 15 (autonomous block 2). Decision: **A for npcs, D for threats**.
  - `active_npcs` replaced with derived view: new `get_recently_active_npcs(campaign_id, limit=6)` engine helper queries `dnd_npcs ORDER BY last_mentioned DESC LIMIT 6`. Renamed in prompt to "Recently active NPCs". Section omitted entirely when result is empty (no `(none)` rendering).
  - `active_threats` prompt block dropped. Schema column kept (no migration); defer until a real threat model surfaces.
  - Locked principle (saved to memory): **don't render `(none)` placeholder text for advisory context** — placeholder = lie. Apply broadly going forward.
  - 13 new unit tests in `test_dnd_npcs.py` cover empty campaign, single NPC, multi-NPC ordering, limit cap, default limit, re-mention promotes, cross-campaign isolation, defensive limit clamping.

### Virgil personal-assistant side
- **V3** — gog retry logic for failures (fully self-contained).
- **V4** — Calendar event details in chat ("what time is golf?" → direct answer).
- **V8** — `/routing` command (show provider scores + latency). Surfaces real router data.
- **V1** — `/weather` command (OpenWeather data already cached, just expose).
- **V2** — `/golf` command (digest already does this, expose on-demand).

### Multiplayer prep (gated on multiplayer not on data)
- **Phase 6** — Actor name reconciliation. Pre-existing bug, **earned priority during Session 14 play** when Donovan was addressed as "Ruby." Will block multiplayer when friends join.

### Larger items that don't need play data
- **Phase 4 — Narrative state consistency repair.** Post-S9. When the LLM commits a fictional object into narrative history, it carries forward as world-truth. S9 doesn't retroactively repair prior fabrications. Separate subsystem. Build only when observed friction surfaces a clear pattern.
- ~~**`weapon_schema.py` extraction.**~~ ✅ SHIPPED Session 15 (autonomous block 2). New module `weapon_schema.py` owns `WeaponCapability` + `WEAPON_CAPABILITIES`. `dnd_orchestration` re-exports for backward compatibility (no caller code changed). `skeleton_loader.get_player_capabilities` now imports from `weapon_schema` directly instead of lazy-importing from orchestration. The coupling loop (skeleton ↔ orchestration through schema) is broken; future DDB ingestion joins as a third consumer of the neutral schema. All 557+ tests pass post-refactor.
- **S20 — `dnd_items` ledger** if S14 (DDB inventory bridge pattern) is decided. The decision itself is the gate, not play data.

### Track 3 follow-ups (extension of Session 14's directive layer)
- **Proactive NPC directive.** Pacing directive at climax tier already says "an NPC must commit." That cashes a check the system can't write — there's no orchestration layer that *causes* an NPC to commit. Would need to surface NPC motivation/agenda data (skeleton parses NPCs but motivation extraction is unclear). Larger ship; needs scoping.
- ~~**Consequence surfacing directive.**~~ ✅ SHIPPED Session 16. v1 spec'd, reviewed, and shipped end-to-end. `dnd_consequences` ledger + dual-pass parser + severity-cap-3 directive + promotion-with-distribution-check + `/consequence list` debug. See `CONSEQUENCE_SURFACING_SPEC.md` for the locked design. Tuning surfaces (promotion thresholds 3/2/10, parser precision, severity calibration) move to log-driven tuning post-deploy.
- **Pacing tier threshold tuning.** The 25/60/85 thresholds are calibrated by intuition. After observing `pacing_directive:` log lines, look at what tiers fired vs subjective scene quality. Same tuning regime as the new consequence promotion thresholds (`consequence_promoted` log lines).
- **Consequence parser tuning.** Two log streams to watch post-deploy: `consequence_captured` (volume + source split — heavy player-side captures or zero DM-side captures may indicate prompt drift) and `consequence_rejected` (high `unresolved_target` rate suggests parser is naming entities that don't exist in canon; high `summary_too_long` suggests the parser ignores the cap). Adjust prompts before adjusting code.
- **Philosophy doc evolution.** The starter doc is opinionated but generic. Jordan should tune it across multiple sessions; expect this to be a living artifact.

### Candidate next layers (filed, not sequenced)

These address THE_GOAL.md failure modes that consequence surfacing v1 does NOT close. The first three (Committed Action Resolution, Combat Initiation Orchestration, Combat Persistence Directive) DO have explicit sequencing because they share the pre-friends-play gating function — see "Pre-friends-play gating bar" section above. Everything below them re-orders based on first-friends-session log data, not pre-design — sequence proposals across unbuilt specs are dependency trees, not commitments.

- ~~**Committed Action Resolution (godmode escape — pre-friends gating ship #1).**~~ ✅ SHIPPED Session 19. Escape-only v1: gating on COMBAT prior intent + scene-shift current intent + Avrae buffer undrained + reaction verbs absent + no explicit retraction → imperative three-option directive (resolve / refuse / charge) with B2.1 narration mandate baked in. New `compute_commitment_directive` directive function plus three regex helpers (`SCENE_SHIFT_RX`, `_REACTION_VERBS`, `_RETRACTION_RX`). Schema: `last_dm_response TEXT DEFAULT ''` on `dnd_scene_state` (one-column migration, idempotent). Single writer `update_last_dm_response` runs between cleaned narration emission and turn-counter increment. Telemetry: `commitment_directive:` per-turn aggregate (every turn, gate signals included for empirical baseline), `commitment_retraction_filtered:` only when retraction suppresses, `directive_emit: commitment=` populated for real. 82 assertions in `test_commitment_directive.py`. **Sibling work NOT in this ship:** layer-3 init orchestration in Combat Initiation Orchestration; layer-4 mode-flip deferred with §11.4 per locked review.
- **Combat Initiation Orchestration (pre-friends gating ship #2).** Spec DRAFTED + decisions LOCKED Session 20. Sibling to committed-action escape. Addresses the `<None>: Dealt N damage!` failure mode visible in Session 18 testing: when a player commits combat in exploration mode, the LLM correctly emits `!attack <weapon> -t <target>` syntax but Avrae has no active initiative tracker, so the `-t TARGET` flag binds to nothing and the attack rolls against `<None>`. **Locked Shape B** (LLM-emits): extend the existing B2.1 attack-template directive with `!init begin` + `!init add <target>` prefix lines (3 commands total in order, narration mandate preserved). No new bot→Avrae write boundary; reactive mode flip via existing path. Implementation: 3-5h Sonnet, no schema changes, ~3-4 files touched (orchestration helpers + directive body, dnd_engine wiring + telemetry, RollDecision branch extension, new `test_init_directive.py`). Spec: `COMBAT_INITIATION_ORCHESTRATION_SPEC.md`. Review: `COMBAT_INITIATION_ORCHESTRATION_REVIEW.md`.
- **Combat Persistence Directive (pre-friends gating ship #3).** Filed Session 19. Bridge-layer directive that reads Avrae's mechanical combat state and constrains narration to honor it. Closes three failure modes: (a) enemies vanishing because the LLM loses the thread mid-fight ("you walk back to town" while a goblin chief still has 30 HP), (b) condition awareness (frightened/paralyzed/grappled characters narrated as if able to act normally), and (c) initiative-order enforcement during active combat — when `dnd_combat_state.active_turn_controller` is populated, narration responds to that controller's input, not to whoever typed last. Addresses both the THE_GOAL "combat shouldn't dismiss enemies" implication and the "fastest typist gets the spotlight" multiplayer failure mode. Same architectural pattern as pacing/central-thread/consequence/commitment — pure function reading mechanical state, returning a constraint string.
- **Encounter balance / mechanical realism.** Filed Session 19. POST-friends — solo testing won't generate the "we considered attacking X" decision corpus needed to spec this correctly. Targets THE_GOAL "I want combat to follow the rules of the world" — level 1 party walking up to a dragon should be warned, in fiction, that this is suicide before initiative locks in. Three sub-layers: (1) **party-capability awareness** — surface aggregate party-level/effective-CR into the prompt so the DM has structured awareness of what the party can survive (data already in `dnd_characters`, just not aggregated); (2) **encounter declaration** — when a fight starts, the DM either prompts for or generates structured stat blocks for enemies (CR, HP, AC), enforcing 5e DMG encounter difficulty math at scene-creation time; (3) **consequence telegraphing** — when a player declares combat against an enemy whose CR exceeds party-survivable threshold, the directive forces the DM to surface the mechanical reality before initiative locks in (perception check, in-fiction warning), without overriding player agency. Same three-layer 5e doctrine: Avrae owns the math, bridge layer enforces narrative consistency with the math, narrative coherence layer informs.
- **NPC introduction-race resolution.** Filed for v2 if `consequence_race:` log volume shows the introduction-race pattern is routine rather than rare. Possible fix shapes: queue rejected proposals for retry after `npc_extractor` thread completes, OR thread in-flight new NPCs from `npc_extractor` into `apply_consequence_proposals`, OR sequence the post-narration extraction order (NPCs first, consequences second). Don't spec until the diagnostic gives signal. Companion `consequence_race: ... reason=npc_introduction_race` diagnostic shipped Session 16 follow-up for empirical baseline.
- **Reputation / faction layer.** Group-level memory, not just individuals. Builds on consequence surfacing — wronging a member of the Red Sash thieves' guild should propagate to the guild, not just the individual. Needs `dnd_factions` table + faction membership + faction-scoped surfacing.
- **Curiosity reward layer.** Hidden lore, attentiveness rewards, lies-pieced-together. Different mechanism (knowledge tier). Targets THE_GOAL "the world should reward curiosity" + "an NPC who lied weeks ago".
- **Item meaningfulness layer.** Lira's signet ring should feel different from a +1 ring of protection. Items meaningfulness layer. Targets THE_GOAL "I want unique items to feel meaningful."
- **Memory tiering / arc summarization (Phase 4).** The deferred Phase 4 spec; needs the above to know what's load-bearing for summarization. v2 of consequence surfacing's "DB is source of truth, prompt is projection" direction lands here.
- **Skeleton-pre-authored consequences.** Extension to consequence v1: allow `skeleton.md` to declare initial consequence state at session 0 (e.g. "the Red Sash already wants you dead"). Currently consequences are forward-only from capture.
- **Scene Lifecycle v1 — post-checkpoint motion-system candidate.** Addresses stagnation drift (F-54) at the parent failure: scene immortality. Deterministic compression triggers (no new info in N exchanges → compress; objective achieved → retire location; NPC purpose resolved → exit NPC; player verbs repeat without escalation → inject change/event/reveal). Pure-function-in-orchestration sibling per Doctrine §59 — reads scene_state, returns a "this scene should retire / compress / transition" signal that becomes a directive the LLM is bound to honor through the same adjudication pattern Track 7 established. **Filed as the second post-checkpoint runtime ship** (after Track 4 #3 Time Progression). No corpus dependency for v1 — ships from architectural reasoning + observed friction. **Encounter Cadence findings doc** at `/home/jordaneal/virgil-docs/track5_findings_encounter_cadence.md` is available as a candidate input for spec-time consideration (~36% strict precision, six categories of encounter cadence shifts, 170 records across 140 episodes). Not currently mapped to a specific runtime ship; encounter-cadence and scene-retirement-trigger concepts are adjacent — spec-drafting prompt for Scene Lifecycle v1 should consider whether the findings doc informs scene-retirement-trigger design. Not a hard dependency. If a compression-cadence corpus findings doc exists by spec time it also informs v1; if not, v1 ships unblocked. Out of scope for v1: visible motion (faction/reputation/relationship state) is downstream texture — v1.x territory shaped by what v1's logs surface. Closes scene-immortality parent of multiple downstream stagnation symptoms (motif compulsion F-52, NPC linger, advancement starvation are all downstream of "the scene never ends").
- **Combat Playability Cluster (Track 6 #5 — addresses F-55).** An intentional shift from CLI-faithful Avrae orchestration (player constructs commands) to structured tactical interface (player selects intent; system constructs commands). Doctrine §1a (Avrae owns mechanics, LLM never decides outcomes) preserved unchanged — what shifts is the player's surface. Affordances-only constraint cluster-wide: Turn Card and resolver list options, never recommend tactical choices (recommending moves removes player agency; listing options preserves the tactical decision-making that is the gameplay). Dependency chain: #5.1 is independent and ships first if entry-friction is the immediate priority; #5.4 is the load-bearing primitive that #5.2 and #5.3 both consume and must ship before either; #5.2 and #5.3 are interchangeable in order once #5.4 lands. Cluster runs in parallel to motion-system thread — neither thread blocks the other.
  - **#5.1 Combat Entry Assist.** DM types monster name (vague or SRD-approximate); bot parses init-embed, identifies un-statted combatants, posts stat-block suggestion to `#dm-aside` for DM approval. Per Doctrine §1b, the SRD fuzzy match uses a small targeted LLM call (structured output: candidate monster name + confidence score) gated by SRD-index validation (proposed candidate must exist in `monster_index.json`). DM approves the proposed `!init madd` command in `#dm-aside` before it fires. LLM suggests; index validates; DM approves; Avrae executes.
  - **#5.2 NPC Turn Automation.** Bot drives enemy turns when `active_turn` is an NPC: selects action, constructs Avrae command, posts to `#dm-narration`. Per Doctrine §1a, target selection and attack-choice logic stay fully deterministic. Lowest-HP / closest-in-init / threat-prioritized rules drive enemy turns. LLM is restricted to narration flavor only — describing the attack, not deciding it. This is the cluster's strictest §1a surface.
  - **#5.3 Combat Cockpit (Turn Card).** When it's a player's turn, bot posts a Turn Card to `#dm-aside` summarizing available actions, remaining resources, and active conditions. Per Doctrine §1a, Turn Card lists affordances only — available actions and resources surfaced from cached character sheet via deterministic rendering. No LLM involvement at this layer; the resolver (#5.4) handles the §1b suggestion surface for target disambiguation when player intent is ambiguous.
  - **#5.4 Intent-to-Avrae Resolver.** Player narrates intent ("attack the wounded goblin"); bot parses into Avrae command and posts to `#dm-narration`. Per Doctrine §1b, target disambiguation uses a small targeted LLM call when intent is ambiguous (multiple valid targets in init list). LLM proposes target with reasoning; init-list validator confirms target exists; player confirms before binding. Deterministic fallback (clarifying-prompt to player) when LLM call fails or confidence is low.

### Filed from Session 18 live verification

- **`bad_name_format` validator drops legit titled NPCs.** `_NAME_RE = ^[A-Z][\w'\-]+(\s+[A-Z][\w'\-]+){0,2}$` requires every word capitalized. "Garrik the Younger", "Hilda the Brewer", "John of Stonebridge", "Marcus von Helder" all fail and are silently dropped from the NPC table. Surfaced live when the parser extracted "Garrik the Younger" but the validator rejected it. **Possible fix:** allow lowercase connector tokens (`the`, `of`, `von`, `de`, `da`, `der`) between capitalized name parts. Cheap regex extension; defense against noise (e.g. "the merchant", "an old man") preserved by maintaining the leading-capital requirement and the 3-word cap. Single-file change in `npc_extractor.py`.
- **Prompt size variance correlates with empty-narration failure (Session 18 S24 data).** Live S24 readings show 24-25k char prompts on every turn in campaign 17. The empty-narration failure mode at 11:44 had `prompt_chars=23789` — within the same range as turns that narrated successfully. Suggests bloat is variance-driven, not threshold-driven. The biggest single block in `directives=` is the philosophy doc at 3,343 chars (33% of directives). **Possible levers:** prune `dm_philosophy.md`; investigate retrieval block growth; add per-section truncation if `total > N`. None ship without empirical signal across more sessions — the S24 stream is the data source. Do NOT auto-trim until a clear bloat-source emerges from logs.
- **DM auto-correction dampens player-typo-driven fragmentation rate.** Session 18 S23 testing showed the LLM auto-corrects player typos in narration ("Stumbling Stage" → "Stumbling Stag", "Stonebrige" → "Stonebridge") before the parser sees the text. The strict-equality identity rule survives in practice partly because the LLM does soft matching itself. Real fragmentation risk is **DM-emitted variants across turns** (less frequent than feared). Useful baseline; no action needed unless `npc_near_match` log volume eventually shows the pattern is real.
- **Surname-addition fragmentation is below S23's distance threshold.** Live S23 testing surfaced that the parser writes both `Lira` and `Lira Songheart` as separate canonical NPCs in the same campaign — single-word vs surnamed variants of the same character. Levenshtein distance between `Lira` and `Lira Songheart` is 10 (space + 9 chars), well past S23's distance ≤2 threshold. **Possible diagnostic shape:** a token-prefix-match check (e.g. when a new canonical name's first token equals an existing canonical name's full string, log `npc_token_prefix_match: new='Lira Songheart' existing='Lira'`). Different code path than levenshtein, different fragmentation pattern. File for next observability batch if the surnamed-vs-bare pattern shows up repeatedly in `dnd_npcs` health reports.
- **Avrae target binding requires combat initiative — `<None>` failure mode is downstream of godmode.** Session 18 B2 verification surfaced the full attack-vanishes failure chain has four layers: (1) LLM emits narration before command — fixed in B2.1. (2) LLM emits correct Avrae syntax — fixed in B2/B2.1. (3) Avrae binds `-t TARGET` to a real combatant — REQUIRES initiative tracker (`!init begin` + `!init add Garrick`). (4) Combat mode auto-flips when player commits violence — REQUIRES the committed-action spec. Layers 1-2 are our orchestration layer (now fixed). Layers 3-4 are addressed by `COMMITTED_ACTION_RESOLUTION_SPEC.md` which orchestrates init management + mode flip + target tracker insertion. Until that spec ships, attacks land at the syntax level but bind to `<None>` at the Avrae level — symptom is correct-looking `!attack` commands followed by Avrae embeds showing `<None>: Dealt N damage!`. **Companion observation:** Avrae state from prior sessions can leak (`throx` showed up as a stale combatant during testing) — worth a `/init end` cleanup at the start of each session, or eventually an automatic reset on campaign load.

### Track 4 — Beat extraction (CRD3-focused after corpus inventory)
- **CRD3 beat-category discovery (hand-extraction).** Sample 10 episodes, hand-categorize 100-200 substantive Matt turns into proposed beat types (scene set, NPC pressure, complication injection, consequence framing, reveal timing, travel compression, etc.). Output: a real taxonomy backed by data. **This is the Track 4 critical path.**
- **CRD3 beat extractor.** Walk substantive Matt turns, capture surrounding context window (N player turns before, N after), tag with classifier from above taxonomy, index by situation. Don't build until taxonomy exists.
- **FIREBALL repurposing.** Inventory revealed FIREBALL is moments not sequences — useful for "what kind of utterance precedes a fireball cast?" patterns, not orchestration learning. Lower priority; specific use case.
- **Compression cadence — corpus extraction candidate (filed, not next).** Extract how real DMs compress low-value continuity (roads, shopping, repeated tavern exchanges, dead travel time, room-to-room walking, redundant searching). Directly feeds Scene Lifecycle v1 and the broader anti-stagnation work — gives motion-system ships empirical targets instead of theory. **Status:** corpus side has hard-stopped after Loot/Reward findings ship until Track 4 #3 runtime feedback lands. Compression cadence is NOT next on the corpus extractor menu — filed as a candidate the corpus side may pick after Loot/Reward findings + Track 4 #3 logs accumulate. Joins the candidate menu without promotion to next-pick. NPC/Factions and Story Progression ruled out as not-next; FIREBALL filed; compression cadence sits alongside.

### Infrastructure (background)
- Migrate cron jobs to systemd timers (cosmetic; cron works fine — defer until friction)
- ~~Add `chroma_dnd` to nightly backup rsync~~ already done (verified Session 15: `crontab -l` line `0 3 * * * rsync -a ... /mnt/virgil_storage/chroma_dnd/ /mnt/backup/chroma_dnd/`)
- Add aliases for new files (philosophy loader, corpus_inventory) — MINGW64-side aliases, requires PC update

### Truly gated on play data
- **Phase 4** — Memory and arc. Premature without 5+ real sessions of friction.
- **S10** — Hint parser recall calibration. Wants real-session phrasings.

---

## Architectural rules (preserve at all costs)

- One authority per concern
- Deterministic writes only
- LLMs advisory, never authoritative
- Retrieval supports memory, not canon
- Parser outputs validated before persistence
- Campaign skeletons outrank parser inference
- Background enrichment preferred over blocking narration
- Keep systems inspectable/debuggable
- Small authoritative subsystems > one mega-prompt
- **Two independent gates before destruction** (Session 13). Hard-delete commands require BOTH archive status AND a typed confirmation phrase. Active state is structural protection; the phrase is human protection.

These hold across all priority decisions below.

---

## Next priorities (post-Session 13)

### Master/code drift reconciliation — ✅ SHIPPED Session 13

Two drift items closed:
- `classify_action_intent` is now mode-aware. Signature: `(text, mode='exploration')`. The dead `in_combat` parameter was removed entirely (no longer accepted). Evaluation order shifts per mode: social/downtime puts SOCIAL+CONTESTED before RISKY; travel promotes TRIVIAL; combat/exploration use the original order. META and COMBAT always win regardless of mode.
- `RISKY_RX` hardened with negative lookaheads on `steal` and `sneak` for idiomatic phrases ("steal a glance", "sneak a peek"). Idiom noun list enumerated explicitly so real larceny still matches.
- 37 unit tests (29 calibration cases + 4 signature/shape + 4 regex-level). Closes the "tests claimed in docs but absent on disk" form of drift simultaneously.

### S9 — Equipment grounding (v1) ✅ SHIPPED Session 13

Track 2 entry point shipped. Specific-item strict-equality grounding against (Avrae attacks ∪ skeleton-declared capabilities). Three-state `CapabilityVerdict` enum (CONFIRMED / VALID_BUT_UNCONFIGURED / INVALID-reserved). Skeleton hint layer is additive-only — confirms presence, never blocks or contradicts. UNVERIFIED narration directive on unconfirmed claims tells the DM to mark the item as UNVERIFIED and not treat it as established equipment in subsequent narration.

**Architectural framing locked:** S9 is a constraint-aware narration safety layer over partial projections. NOT a truth engine, NOT a rules engine, NOT a database layer. Truth sources (Avrae attacks, skeleton.md, future DDB) are partial projections; absence of data is NOT evidence of absence.

**Live verified:** dagger and mace test cases produced correct verdicts and log lines. Donovan Ruby's skeleton populated with `shortsword, shortbow, dagger`.

**S9 follow-ups (NOT to be folded back into S9 v1):**
- **Phase 4 — Narrative state consistency repair.** When the LLM commits a fictional object into narrative history (e.g. hallucinated mace from a prior turn), it carries forward as "world truth" via continuation bias. S9 only validates current-turn claims; it does not retroactively repair prior fabrications. This is a separate subsystem with separate scope. Likely shapes: post-turn narration scanning, established-equipment ledger, conversation-history contradiction detection. **Do NOT scope-creep S9 into this.** Build only when observed friction surfaces a clear pattern.
- **`weapon_schema.py` extraction.** Currently `dnd_orchestration` is the schema authority for `WEAPON_CAPABILITIES`; `skeleton_loader` lazy-imports it. Works for v1, but creates a coupling loop. When DDB ingestion arrives or any third capability source, extract weapon-family definitions to a shared module so all consumers depend on a neutral schema, not on orchestration. Filed by Session 13 design review. Refactor before adding the second source.

### Bot startup auto-cache

Cache miss after every bot restart breaks sheet-grounded behavior until someone runs `/refresh`. Currently in-memory only. Three reasonable shapes for fix (cheapest first):
- (a) On bot startup, scan recent active-campaign channel history for Avrae sheet embeds and parse each via `parse_avrae_sheet_embed` + `set_cached_context`. Reuses the same code path `/refresh` uses, just automatic.
- (b) Persist `_CHARACTER_CACHE` to SQLite so it survives restarts.
- (c) Listen passively in `on_message` for Avrae sheet embeds and auto-populate.

Lean (a). Surfaces from Session 13 S9 live verification — `/refresh` requirement is friction.

### Ship 5 — Orchestration / pacing intelligence

Session 12 surfaced "keep walking" hell — the LLM doesn't compress travel naturally and lacks scene-clock / beat-pacer awareness. `/travel` addresses the worst case (player wants to skip ahead). Underlying problem: there's no orchestration layer that says "given quest state + scene mode + location + time elapsed, what kind of beat should this turn be?"

Gated on more real-session data. Pacing intelligence has too many possible shapes and the right one comes from observed patterns, not architecture-first design. Pacing intelligence is Track 3 work (narrative simulation) and the design tracks specifically prohibit jumping ahead before canonical identity stabilizes.

**If a play session has clear value (e.g. "we don't have enough Ship 5 signal"), play. Otherwise build other queue items.**

### Phase 4 — Memory and Arc

Premature to design. Run a few real sessions on the working stack first; let the actual friction shape what memory tiering needs to solve. Session 9 lesson: anticipated friction misleads, observed friction informs.

Likely shapes when we get there:
- 4.1 Memory tiering — separate recent session, campaign summaries, major events, NPC relationship summaries. Stop dumping raw history into the prompt.
- 4.2 Campaign arc summaries — every few sessions, generate "Previously on..." faction shifts, unresolved hooks.

Build these AFTER 5+ real sessions on the Phase 12 stack.

### Phase 6 — Actor Name Reconciliation (gate: before friends join)

Surfaced by 2B.1 logs in Session 8: Avrae says 'throx', Virgil batches as 'Donovan Ruby', cache key is something else again. Three names for one entity. `buffer.consume` returns 0 events because the actor name doesn't match the buffer.

Solo doesn't surface this — only one character at the table. Multiplayer will surface it badly. Fix before inviting friends, not while they're waiting.

- 6A: Decide canonical form (probably Avrae sheet name lowercased, since that's authoritative for mechanics).
- 6B: Reconciliation layer in `dnd_orchestration`: name lookup that handles Avrae name ↔ display name ↔ cache key.
- 6C: Live verify with two characters bound.

---

## Small wins (parallel-safe)

### DnD
- **S1** — `/scenestate` DM peek command (tension/mode/clocks without sqlite3)
- **S2** — `/clock` auto-tick on roll outcomes
- **S3** — `active_npcs` / `active_threats` writers (currently nothing populates these)
- **S4** — Tension display on `/campaigns`
- **S5** — ✅ SHIPPED Session 11. Long-rest auto-mode-flip via `_handle_rest_event`.
- **S6** — Phase 3 polish: MODE auto-execute undo placeholder cleanup
- **S7** — Phase 3 polish: `parse_auto_execute` quest dedup against existing log
- **S8** — Scene-seed authority — `/play` seed currently treated as flavor, not authoritative scaffolding. Strengthen system prompt to make seed elements required.
- **S9** — ✅ SHIPPED Session 13. Equipment grounding (Track 2 entry point). 3-state CapabilityVerdict + skeleton hint layer + UNVERIFIED narration directive. Strict full-string equality, no inference. Live verified.
- **S10** — Hint parser recall calibration — after 5+ real sessions, review `hint_parse` logs for phrasings that returned `[]` when bookkeeping was implied.
- **S11** — [DEFERRED to Ship 5] DM-driven turn pacing.
- **S12** — ✅ SHIPPED Session 11. Phase 11.1 syntax correction.
- **S13** — Investigate cloud_router scoring on extraction task. Designed candidate order `['cerebras','groq','local']` but live calibration shows groq winning SCORE_SORT (score 0.30-0.31). Confirm whether cerebras was cooled down or score-sort overrides declared order.
- **S14** — Inventory bookkeeping bridge. **Direction locked Session 15: option (c) — suggestion-only first.** No DDB writes, no Virgil-side ledger until observed friction shows the suggestion-only pattern is insufficient. Implementation deferred until then.
  - ~~(a) DDB API direct write~~ rejected for v1
  - ~~(b) Virgil-side ledger~~ deferred (this is what S20 would have built)
  - **(c) Suggestion-only**: prompt block says "Add to DDB: Healing Potion" — DM types it manually if they want it tracked. Pure suggestion, no automation.

### Phase 12 follow-ups
- **S15** — `/npc merge <id_a> <id_b>` command. Build only when fragmentation telemetry shows it's worth the surface area. Session 12 baseline: ~50% fragmentation on campaign 17 from full-name vs short-form parser drift. Bounded, not exponential.
- **S16** — ✅ SHIPPED Session 13. Phantom-location heuristic via `phantom_location_candidates()` + `phantom_candidates:` log line.
- **S17** — ✅ SHIPPED Session 13. `world_health` aggregate metric via `world_health_report()` + `world_health:` log line.
- **S18** — `/skeleton propose-diff` — read parser state, generate skeleton.md diff for human review and manual apply. Future tooling per Phase 12 spec. **More valuable now** with phantom telemetry surfacing candidates that may want skeleton promotion (e.g. River Wynd earning its place via mention_count growth).
- **S19** — Skeleton segmentation when single-file outgrows ~10 NPCs / 15 locations / 3 factions per campaign. Likely future shape: `skeleton/core.md`, `factions.md`, `locations.md`, `arc_N.md`. Don't pre-build.
- **S20** — `dnd_items` ledger (Phase 12.1 deferred). Defer until Avrae inventory bookkeeping pattern (S14) settles.

### Phantom-threshold tuning (gated on play data)
Ship 4 telemetry is live with default `threshold=3`. Tuning should be informed by observed re-mention patterns — what threshold value distinguishes Stormbridge-class phantoms from River Wynd-class emergent geography that takes time to be re-referenced. Defer until 3-5 sessions of `phantom_candidates:` log accumulation.

### Virgil
- **V1** — `/weather` command (OpenWeather data already cached)
- **V2** — `/golf` command (digest already does this)
- **V3** — Retry logic for gog failures (2s retry, 10 lines)
- **V4** — Calendar event details in chat ("what time is golf?")
- **V5** — SQLite FTS5 full-text message search (`/search memory "bowling"`)
- **V6** — Periodic fact consolidation (nightly LLM pass over day's messages)
- **V7** — Morning digest archive (save daily instead of overwriting)
- **V8** — `/routing` command (show provider scores + latency)
- **V9** — Self-healing router (exponential decay vs fixed cooldown)
- **V10** — Calendar conflict detection (warn on overlapping events)

### Infrastructure (low priority)
- Migrate cron jobs to systemd timers (cosmetic; cron works fine)
- ~~Add `chroma_dnd` to nightly backup rsync~~ already done (3am cron line in `crontab -l`)
- ~~Add `virgil-discord` to sentinel monitoring~~ ✅ SHIPPED Session 14. `sentinel.sh` checks both `virgil-bot` and `virgil-discord` via `systemctl --user is-active`.

---

## Master/code drift to reconcile

- **mode-aware classify_action_intent** — ✅ SHIPPED Session 13. Signature now `(text, mode='exploration')`. Mode-aware evaluation order. 37 unit tests.
- **RISKY_RX negative lookaheads on steal/sneak** — ✅ SHIPPED Session 13. Negative lookaheads exclude idiomatic uses ("steal a glance", "sneak a peek") universally. Real larceny with concrete objects still matches.
- ~~**`test_check_action_capability.py` asserts old `has_capability` field**~~ ✅ SHIPPED Session 15 (autonomous block). Refactored to use the v1 `verdict` enum throughout. 52/52 tests pass (up from 29/52). Documented strict-equality semantics in the file header so future readers understand why prefix/suffix cases (e.g. `+1 Longsword` vs sword family) are now `VALID_BUT_UNCONFIGURED` not CONFIRMED. Added new tests: skeleton-promotes-to-CONFIRMED, skeleton-does-not-demote-unmatched, INVALID renderer contract (manual decision construction since v1 has no INVALID producer), `equip`/`use` verbs.

---

## Shelved (intentionally, with reasons)

- **Cyberpunk-tone corpus expansion.** Genre-locked retrieval is acceptable until cyberpunk is the primary play mode.
- **Tone-vector corpus tagging** (re-classify 740k docs). Still too expensive vs the kill switch we already have (`USE_KNOWLEDGE_GUIDANCE`).
- **Owlbear Rodeo / map integration.** Theatre of mind plays cleanly.
- **Multi-campaign on Discord.** Single-server multiplayer first.
- **Telegram DnD bot revival.** Discord won the architecture battle.
- **LangChain.** We built retrieval ourselves on ChromaDB and it works.
- **Friends & Fables migration.** Building Virgil IS the project, not the destination.
- **Full inventory parsing.** Use `narrative_tags` instead.
- **Building our own rules engine.** Avrae IS the engine.
- **Custom roll-result parsing pipeline with `[MECHANICS]` tags.**
- **nsjail sandbox, Redis, async architecture, full-file `/patch`.**
- **Whisper voice, email triage, PDF Q&A, TTS output.**
- **Multi-user Virgil (premature), SillyTavern UI (not needed).**
- **Single 0-100 world health score** (Session 13 design choice). Weighting NPC fragmentation against skeleton coverage against phantom count is unjustified speculation; a single number hides the underlying signal. The `world_health:` log line surfaces all four numbers as components.

---

## Decision rules (carry forward — these matter)

1. **The system evolves from observed friction, not anticipated friction.** Ship the smallest change that makes friction visible. Watch usage. Let data drive what's next. NOT: design the full infrastructure that would solve every variant.

2. **Architectural invariants are non-negotiable** (see `VIRGIL_MASTER.md` Section 4):
   - Avrae owns mechanics; Virgil owns narrative.
   - SQLite is authoritative structured state.
   - Cloud router is the only path for LLM calls.
   - LLMs never directly mutate structural state.
   - Mode/tension/clocks/current_location_id/NPCs/locations all have single write paths.
   - Discord transport is a thin shell; no gameplay logic there.
   - Engine owns SQLite; orchestration owns rules-engine logic.
   - Avrae uses message edits as state transitions; always wire `on_message` AND `on_message_edit`.
   - `skeleton_origin=1` rows are authored canon — parsers cannot overwrite.
   - Hard-delete commands require two independent gates (archive + phrase).

3. **The advisory parser pattern.** Five instances now: `extract_scene_updates`, Phase 3 auto-execute, Phase 11.1 mechanical hints, Phase 12A NPC extractor, Phase 12B location extractor. Pattern shape: bounded text input → small LLM → strict structured output → deterministic validator → whitelist-restricted side effect. Reuse pattern; don't proliferate architectures.

4. **One problem fixed at a time, confirmed working, then move on.** Off-roadmap fixes only when the bug is critical AND can't wait for the planned next step.

5. **Diagnostic before treatment.** When unsure: ship the smallest log line that answers the question, then decide. Session 10's two most consequential decisions (kill corpus / model swap) both came from diagnostic data, not theory. Session 13 reinforced: when a phrase comparison failed, the SQL diagnostic on the actual stored bytes (`hex(name)`) confirmed the DB was clean before any fix shipped.

6. **When user states a design preference, that's a constraint not a suggestion.** Don't make architectural calls against stated preferences. (Section 7 — `USE_KNOWLEDGE_GUIDANCE = False` precedent.)

7. **Two independent gates before destruction** (Session 13). Any hard-delete command requires BOTH structural state (archived) AND human confirmation (typed phrase). Either gate alone is insufficient. Belt-and-suspenders explicitly applies to data loss.

---

## Design priority — solo vs multiplayer

Solo and multiplayer are EQUALLY important. Multiplayer may be more important. The architecture must support both without a rewrite. Features should be built with multiplayer in mind from the start, not retrofitted later.

**Multiplayer is NOT deferred.**

The current solo flow exercises canonical entities, scene mode, transitions, autocomplete, and skeleton grounding. The multiplayer-only friction (actor name reconciliation, turn-based action gating verified in production) is what Phase 6 closes before friends join.


ROADMAP_PROBE_XQ7M : testing sync